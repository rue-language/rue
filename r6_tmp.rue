const rawbuf = @import("std/rawbuf.rue");
const Buf = rawbuf.RawBuf(i64);
fn main() -> i32 {
    let mut b = Buf.new();
    b.grow_to(4);
    b.write_at(0, 42);
    let v = b.read_at(0);
    b.free();
    if v == 42 { 0 } else { 1 }
}
