// Test that addition order is preserved correctly
fn main() -> i32 {
    let a: i32 = 1 + 2 + 3;  // Should be (1 + 2) + 3 = 6
    let b: i32 = 1 + (2 + 3);  // Should be 1 + 5 = 6
    a + b  // Should return 12
}