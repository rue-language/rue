//!@ id T-BLOCK-SCOPE-001
//!@ kind compile-fail
//!@ spec §4.1.2 Block Scopes
//!@ expect stderr "undefined"

fn main() -> i32 {
    // Block scopes not implemented as standalone features yet
    // This would fail with undefined variable
    let y: i32 = 20;
    x  // Error: x is undefined
}