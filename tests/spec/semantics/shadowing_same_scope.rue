//!@ id T-SCOPE-SHADOW-001
//!@ kind run-pass
//!@ spec §4.1.3 Variable Shadowing
//!@ expect exit 20

fn main() -> i32 {
    let x: i32 = 10;      // First x
    let x: i32 = 20;      // Second x shadows first x
    x                     // Returns 20 (first x is no longer accessible)
}