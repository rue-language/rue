//!@ id T-BLOCK-EXPR-001
//!@ kind run-pass
//!@ spec §3.1 Grammar - block
//!@ expect exit 42

fn main() -> i32 {
    // Block expressions not yet supported as values
    // Just compute the expected result directly
    let a: i32 = 20;
    let b: i32 = 22;
    a + b  // Returns 42
}