const model = @import("model.rue");

pub fn compute(value: i32) -> i32 {
    let mut total = 0;
    let mut index = 0;
    while index < 2 {
        total = total + model.weight(value);
        index = index + 1;
    }
    total / 2
}
