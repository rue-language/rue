════════════════════════════════════════════════════════════════════════════════
 destructure_one_arm
════════════════════════════════════════════════════════════════════════════════
A destructure in one arm of an if only: the §5.5 join meets MovedOut against
Owned at a declared-linear place and is ill-formed (E0443); the taken path
runs.

Rules exercised: (Use-Declared-Linear-Destructure) §5.1 · (If) §5.5 join · 3.8:50

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
linear struct S11 { x0: i64, x1: S1 }
struct S12 { x0: S11, x1: S1 }
linear struct S13 { x0: S11, x1: S1 }
linear struct S14 { x0: S1, x1: S1 }
linear struct S15 { x0: S1, x1: i64, x2: S1 }
linear struct S16 { x0: S8, x1: S1 }
linear struct S17 { x0: i64, x1: S2 }
linear struct S18 { x0: i64, x1: S1 }
drop fn S18(self) { @dbg(self.x0); }
struct S19 { x0: i64, x1: S2 }
linear struct S20 { x0: S19, x1: S1 }
linear struct S21 { x0: [S1; 2], x1: i64 }
linear struct S22 { x0: [S1; 2], x1: S1 }
fn f0() -> i64 {
    {
        let v0: S11 = S11 { x0: 1, x1: S1 { x0: 2 } };
        {
            let v1: i64 = if true {
                v0.x0
            } else {
                0
            };
            v1
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (If) §5.5 join:

    on  if true { v0.x0 } else { 0 }

    the two arms disagree on a linear-carrying entry — v0: S11 is MovedOut in
    the then-arm and Owned in the else-arm, so a linear value is consumed on
    only some paths ((If) premise `Σ' = join(Σ1, Σ2)`, §5.5; 3.8:50; the
    compiler reports E0443)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S11 = S11 { x0: 1, x1: S1 { x0: 2 } }; { let v1: i64 = if true { v0.x…
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S11 { x0: 1, x1: S1 { x0: 2 } }
        Γ;Σ     (empty)
        ⇒       S11  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: i64 = if true { v0.x0 } else { 0 }; v1 }
        Γ;Σ     [v0: S11 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (If) §5.5 join
            e       if true { v0.x0 } else { 0 }
            Γ;Σ     [v0: S11 = Owned]
            ✗       this premise fails:
                    the two arms disagree on a linear-carrying entry — v0: S11 is
                    MovedOut in the then-arm and Owned in the else-arm, so a linear
                    value is consumed on only some paths ((If) premise `Σ' = join(Σ1,
                    Σ2)`, §5.5; 3.8:50; the compiler reports E0443)
              (Lit) §5.8
                e       true
                Γ;Σ     [v0: S11 = Owned]
                ⇒       bool  ⊣  [v0: S11 = Owned]
              (Use-Declared-Linear-Destructure) §5.1
                e       v0.x0
                Γ;Σ     [v0: S11 = Owned]
                ⇒       i64  ⊣  [v0: S11 = MovedOut]
              (Lit) §5.8
                e       0
                Γ;Σ     [v0: S11 = Owned]
                ⇒       i64  ⊣  [v0: S11 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d4          (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 2 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 2 }#0
  [5]   d3        (D-Struct) §6.5 (mint #1)
         expr    S11 { x0: 1, x1: S1 { x0: 2 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value S11 { 1, S1 { 2 }#0 }#1
  [6]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S11 { 1, S1 { 2 }#0 }#1 at ℓ2
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S11 { 1, S1 { 2 }#0 }#1]
         result  value S11 { 1, S1 { 2 }#0 }#1
  [7]   d5            literal §6.3
         expr    true
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S11 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S11 { 1, S1 { 2 }#0 }#1]
         result  value true
  [8]   d5            (D-Use-Declared-Linear) §6.3
         expr    v0.x0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S11 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘]
         events  >> drop ℓ2 = S1 { 2 }#0; run drop fn S1(S1 { 2 }#0); consume S11 { ⊘, ⊘ }#1
         result  value 1
  [9]   d4          (D-If-T) §6.6
         expr    if true { v0.x0 } else { 0 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S11 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘]
         result  value 1
  [10]  d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = 1 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = 1]
         result  value 1
  [11]  d4          (D-Use-Copy) §6.3
         expr    v1
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = 1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = 1]
         result  value 1
  [12]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ3])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = 1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = †]
         result  value 1
  [13]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 1
  [14]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 1

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [4]     [8.1] drop ℓ2                     [8]         once
  #1    [5]     [8.2] consume                     —           once

  Every owned identity is ended exactly once (`drop_exactly_once`).

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 1
         drop trace: drop ℓ2 = S1 { 2 }#0; run drop fn S1(S1 { 2 }#0); consume S11 { ⊘, ⊘ }#1

