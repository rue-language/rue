════════════════════════════════════════════════════════════════════════════════
 match_never_first_arm
════════════════════════════════════════════════════════════════════════════════
A match whose first arm is a return and whose second is true: (Sub-Never)
coerces the return to bool, so the match is bool inside an i64 function. The
checker once read the arms' type off the first arm and refused; it now takes
the first arm that has a type. The run takes K1: true, then 5.

Rules exercised: (Match) §5.5 · (Sub-Never) §5.7 · (Return-Value) §5.7 · (Dbg) §5.8

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
struct S11 { x0: E0, x1: S1 }
enum E0 { K0(S1), K1 }
enum E1 { K0(S3), K1 }
enum E2 { K0(S1, S1), K1 }
enum E3 { K0, K1 }
enum E4 { K0(i64), K1 }
enum E5 { K0(S1, S3), K1 }
fn f0() -> i64 {
    {
        let v0: bool = match E3.K1 {
            E3.K0 => {
                return 7
            },
            E3.K1 => {
                true
            },
        };
        {
            { let t3g: bool = v0; @dbg(t3g) };
            5
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: bool = match E3.K1 { E3.K0 => return 7, E3.K1 => true }; { @dbg(v0); …
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Match) §5.5 join
        e       match E3.K1 { E3.K0 => return 7, E3.K1 => true }
        Γ;Σ     (empty)
        ⇒       bool  ⊣  (empty)
          (Enum-Intro) §5.5
            e       E3.K1
            Γ;Σ     (empty)
            ⇒       E3  ⊣  (empty)
          (Return-Value) §5.7
            e       return 7
            Γ;Σ     (empty)
            ⇒       never  ⊣  ⊥
              (Lit) §5.8
                e       7
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
          (Lit) §5.8
            e       true
            Γ;Σ     (empty)
            ⇒       bool  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @dbg(v0); 5 }
        Γ;Σ     [v0: bool = Owned]
        ⇒       i64  ⊣  [v0: bool = Owned]
          (Dbg) §5.8
            e       @dbg(v0)
            Γ;Σ     [v0: bool = Owned]
            ⇒       ()  ⊣  [v0: bool = Owned]
              (Use-Copy) §5.1
                e       v0
                Γ;Σ     [v0: bool = Owned]
                ⇒       bool  ⊣  [v0: bool = Owned]
          (Lit) §5.8
            e       5
            Γ;Σ     [v0: bool = Owned]
            ⇒       i64  ⊣  [v0: bool = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          (D-Enum-Intro) §6.6 (mint #0)
         expr    E3.K1
         store   (empty)
           →     [ℓ0 = †]
         result  value E3.K1⟨⟩#0
  [3]   d4          (D-Match) §6.6 (bind the arm's payload)
         expr    bind E3.K1's payload to []
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value E3.K1⟨⟩#0
  [4]   d4          literal §6.3
         expr    true
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value true
  [5]   d3        (D-EndScope) §6.6 (end the arm)
         expr    endscope([])
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value true
  [6]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = true at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = true]
         result  value true
  [7]   d5            (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = true]
           →     [ℓ0 = †, ℓ1 = true]
         result  value true
  [8]   d4          (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(v0)
         store   [ℓ0 = †, ℓ1 = true]
           →     [ℓ0 = †, ℓ1 = true]
         events  >> @dbg prints true
         result  value ()
  [9]   d4          literal §6.3
         expr    5
         store   [ℓ0 = †, ℓ1 = true]
           →     [ℓ0 = †, ℓ1 = true]
         result  value 5
  [10]  d3        (D-Seq) §6.7
         expr    { @dbg(v0); 5 }
         store   [ℓ0 = †, ℓ1 = true]
           →     [ℓ0 = †, ℓ1 = true]
         result  value 5
  [11]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ1])
         store   [ℓ0 = †, ℓ1 = true]
           →     [ℓ0 = †, ℓ1 = †]
         result  value 5
  [12]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †]
         result  value 5

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 5
         drop trace: @dbg prints true

