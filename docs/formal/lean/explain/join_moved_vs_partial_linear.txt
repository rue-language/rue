════════════════════════════════════════════════════════════════════════════════
 join_moved_vs_partial_linear
════════════════════════════════════════════════════════════════════════════════
One arm drops a linear-carrying S10 whole, the other drops only its affine x1
and leaves the linear x0 Owned: §5.5's join of MovedOut with that partial
state is undefined (3.8:50; the compiler reports E0443). The run takes the
second arm and meets the live x0 at the scope exit: linearLeak. Seeded by the
definition mutants (RUE-2465): a join without the residual check passed every
seed and generated case, and only a proof noticed it.

Rules exercised: (If) §5.5 · §5.5 join · 3.8:50 · (@Drop) §5.3 · §5.6 residual-linear leak check

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S10 = S10 { x0: S3 { x0: 1 }, x1: S1 { x0: 2 } };
        {
            if false {
                @drop(v0)
            } else {
                @drop(v0.x1)
            };
            9
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (If) §5.5 join:

    on  if false { @drop(v0) } else { @drop(v0.x1) }

    the two arms disagree on a linear-carrying entry — v0: S10 is MovedOut in
    the then-arm and Owned{ x0: Owned, x1: MovedOut } in the else-arm, so a
    linear value is consumed on only some paths ((If) premise `Σ' = join(Σ1,
    Σ2)`, §5.5; 3.8:50; the compiler reports E0443)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S10 = S10 { x0: S3 { x0: 1 }, x1: S1 { x0: 2 } }; { if false { @drop(…
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S10 { x0: S3 { x0: 1 }, x1: S1 { x0: 2 } }
        Γ;Σ     (empty)
        ⇒       S10  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S3 { x0: 1 }
            Γ;Σ     (empty)
            ⇒       S3  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { if false { @drop(v0) } else { @drop(v0.x1) }; 9 }
        Γ;Σ     [v0: S10 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (If) §5.5 join
            e       if false { @drop(v0) } else { @drop(v0.x1) }
            Γ;Σ     [v0: S10 = Owned]
            ✗       this premise fails:
                    the two arms disagree on a linear-carrying entry — v0: S10 is
                    MovedOut in the then-arm and Owned{ x0: Owned, x1: MovedOut } in the
                    else-arm, so a linear value is consumed on only some paths ((If)
                    premise `Σ' = join(Σ1, Σ2)`, §5.5; 3.8:50; the compiler reports
                    E0443)
              (Lit) §5.8
                e       false
                Γ;Σ     [v0: S10 = Owned]
                ⇒       bool  ⊣  [v0: S10 = Owned]
              (@Drop) §5.3
                e       @drop(v0)
                Γ;Σ     [v0: S10 = Owned]
                ⇒       ()  ⊣  [v0: S10 = MovedOut]
              (@Drop) §5.3
                e       @drop(v0.x1)
                Γ;Σ     [v0: S10 = Owned]
                ⇒       ()  ⊣  [v0: S10 = Owned{ x0: Owned, x1: MovedOut }]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d4          (D-Struct) §6.5 (mint #0)
         expr    S3 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S3 { 1 }#0
  [4]   d5            literal §6.3
         expr    2
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value 2
  [5]   d4          (D-Struct) §6.5 (mint #1)
         expr    S1 { x0: 2 }
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = †]
         result  value S1 { 2 }#1
  [6]   d3        (D-Struct) §6.5 (mint #2)
         expr    S10 { x0: S3 { x0: 1 }, x1: S1 { x0: 2 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value S10 { S3 { 1 }#0, S1 { 2 }#1 }#2
  [7]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
         result  value S10 { S3 { 1 }#0, S1 { 2 }#1 }#2
  [8]   d5            literal §6.3
         expr    false
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
         result  value false
  [9]   d5            @drop §6.11
         expr    @drop(v0.x1)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         events  >> drop ℓ3 = S1 { 2 }#1; run drop fn S1(S1 { 2 }#1)
         result  value ()
  [10]  d4          (D-If-F) §6.6
         expr    if false { @drop(v0) } else { @drop(v0.x1) }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         result  value ()
  [11]  d4          literal §6.3
         expr    9
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         result  value 9
  [12]  d3        (D-Seq) §6.7
         expr    { if false { @drop(v0) } else { @drop(v0.x1) }; 9 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, S1 { 2 }#1 }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         result  value 9
  [13]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    { let v0: S10 = S10 { x0: S3 { x0: 1 }, x1: S1 { x0: 2 } }; { if false { @drop(…
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [14]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = S10 { S3 { 1 }#0, ⊘ }#2]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     —                                 —           the run was refused (§6)
  #1    [5]     [9] drop ℓ3                       [9]         once
  #2    [6]     —                                 —           the run was refused (§6)

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

