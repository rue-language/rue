════════════════════════════════════════════════════════════════════════════════
 match_payload_assign
════════════════════════════════════════════════════════════════════════════════
A match arm assigns its i64 payload binding, which (Match) §5.5 binds
immutable, so (Assign) §5.2 refuses it (the compiler: cannot assign to
immutable). The machine runs the arm: the value is 5. Seeded by the definition
mutants (RUE-2465): mutable payload bindings passed every seed and generated
case.

Rules exercised: (Match) §5.5 · (Assign) §5.2

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
struct S11 { x0: E0, x1: S1 }
enum E0 { K0(S1), K1 }
enum E1 { K0(S3), K1 }
enum E2 { K0(S1, S1), K1 }
enum E3 { K0, K1 }
enum E4 { K0(i64), K1 }
enum E5 { K0(S1, S3), K1 }
fn f0() -> i64 {
    {
        let v0: E4 = E4.K0(4);
        match v0 {
            E4.K0(v1) => {
                {
                    { v1 = 5; };
                    v1
                }
            },
            E4.K1 => {
                0
            },
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Assign) §5.2, 3.8:77:

    on  { v1 = 5; }

    the assignment target is not a `mut` binding ((Assign) mutability side
    condition, §5.2; 5.1:3)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: E4 = E4.K0(4); match v0 { E4.K0(v1) => { { v1 = 5; }; v1 }, E4.K1 => …
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Enum-Intro) §5.5
        e       E4.K0(4)
        Γ;Σ     (empty)
        ⇒       E4  ⊣  (empty)
          (Lit) §5.8
            e       4
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Match) §5.5
        e       match v0 { E4.K0(v1) => { { v1 = 5; }; v1 }, E4.K1 => 0 }
        Γ;Σ     [v0: E4 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Use-Copy) §5.1
            e       v0
            Γ;Σ     [v0: E4 = Owned]
            ⇒       E4  ⊣  [v0: E4 = Owned]
          (Seq) §5.3, 3.8:64
            e       { { v1 = 5; }; v1 }
            Γ;Σ     [v1: i64 = Owned, v0: E4 = Owned]
            ✗       this premise fails:
                    a premise's own derivation failed — the reason is the rejected
                    premise nested under this rule
              (Assign) §5.2, 3.8:77
                e       { v1 = 5; }
                Γ;Σ     [v1: i64 = Owned, v0: E4 = Owned]
                ✗       this premise fails:
                        the assignment target is not a `mut` binding ((Assign) mutability
                        side condition, §5.2; 5.1:3)
          (Lit) §5.8
            e       0
            Γ;Σ     [v0: E4 = Owned]
            ⇒       i64  ⊣  [v0: E4 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    4
         store   (empty)
           →     (empty)
         result  value 4
  [3]   d3        (D-Enum-Intro) §6.6 (mint #0)
         expr    E4.K0(4)
         store   (empty)
           →     [ℓ0 = †]
         result  value E4.K0⟨4⟩#0
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = E4.K0⟨4⟩#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0]
         result  value E4.K0⟨4⟩#0
  [5]   d4          (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0]
         result  value E4.K0⟨4⟩#0
  [6]   d4          (D-Match) §6.6 (bind the arm's payload)
         expr    bind E4.K0's payload to [ℓ2]
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 4]
         result  value E4.K0⟨4⟩#0
  [7]   d6              literal §6.3
         expr    5
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 4]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 4]
         result  value 5
  [8]   d5            (D-Assign) §6.8 (overwrite-drop)
         expr    { v1 = 5; }
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 4]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 5]
         result  value ()
  [9]   d5            (D-Use-Copy) §6.3
         expr    v1
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 5]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 5]
         result  value 5
  [10]  d4          (D-Seq) §6.7
         expr    { { v1 = 5; }; v1 }
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 4]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 5]
         result  value 5
  [11]  d3        (D-EndScope) §6.6 (end the arm)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = 5]
           →     [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = †]
         result  value 5
  [12]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ1])
         store   [ℓ0 = †, ℓ1 = E4.K0⟨4⟩#0, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value 5
  [13]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value 5

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  The run owns no identity: nothing to end.

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 5
         drop trace: (no drops)

