════════════════════════════════════════════════════════════════════════════════
 partial_under_dtor
════════════════════════════════════════════════════════════════════════════════
A field moved out of a value whose type declares a destructor: 3.9:34 forbids
it (E0456), because the destructor runs on the whole value and would observe
the hole.

Rules exercised: (Use-Move) §5.1 · 3.9:34

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S5 = S5 { x0: 1, x1: S1 { x0: 2 } };
        {
            let v1: S1 = v0.x1;
            {
                @drop(v1);
                9
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Use-Move) §5.1:

    on  v0.x1

    a proper prefix of the path has a type that declares a destructor, so the
    field may not be moved out of it — the destructor runs on the whole value
    and would observe the hole ((Use-Move)/(@Drop) premise, §5.1, §5.3;
    3.9:34; the compiler reports E0456)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S5 = S5 { x0: 1, x1: S1 { x0: 2 } }; { let v1: S1 = v0.x1; { @drop(v1…
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S5 { x0: 1, x1: S1 { x0: 2 } }
        Γ;Σ     (empty)
        ⇒       S5  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: S1 = v0.x1; { @drop(v1); 9 } }
        Γ;Σ     [v0: S5 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Use-Move) §5.1
            e       v0.x1
            Γ;Σ     [v0: S5 = Owned]
            ✗       this premise fails:
                    a proper prefix of the path has a type that declares a destructor,
                    so the field may not be moved out of it — the destructor runs on the
                    whole value and would observe the hole ((Use-Move)/(@Drop) premise,
                    §5.1, §5.3; 3.9:34; the compiler reports E0456)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d4          (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 2 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 2 }#0
  [5]   d3        (D-Struct) §6.5 (mint #1)
         expr    S5 { x0: 1, x1: S1 { x0: 2 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value S5 { 1, S1 { 2 }#0 }#1
  [6]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S5 { 1, S1 { 2 }#0 }#1 at ℓ2
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
         result  value S5 { 1, S1 { 2 }#0 }#1
  [7]   d4          (D-Use-Move) §6.3
         expr    v0.x1
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1]
         result  value S1 { 2 }#0
  [8]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = S1 { 2 }#0 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = S1 { 2 }#0]
         result  value S1 { 2 }#0
  [9]   d5            @drop §6.11
         expr    @drop(v1)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = S1 { 2 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = ⊘]
         events  >> drop ℓ3 = S1 { 2 }#0; run drop fn S1(S1 { 2 }#0)
         result  value ()
  [10]  d5            literal §6.3
         expr    9
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = ⊘]
         result  value 9
  [11]  d4          (D-Seq) §6.7
         expr    { @drop(v1); 9 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = S1 { 2 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = ⊘]
         result  value 9
  [12]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ3])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = †]
         result  value 9
  [13]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, ⊘ }#1, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         events  >> drop ℓ2 = S5 { 1, ⊘ }#1; run drop fn S5(S5 { 1, ⊘ }#1)
         result  value 9
  [14]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 9

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity, and the step numbers there are the order `drop_order` fixes;
`[n.k]` is the k-th end of step n, when one step ends several values.

  id    minted  ended                             destructor  ended
  ────────────────────────────────────────────────────────────────────────
  #0    [4]     [9] drop ℓ3                       [9]         once
  #1    [5]     [13] drop ℓ2                      [13]        once

  Every owned identity is ended exactly once (`drop_exactly_once`).

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 9
         drop trace: drop ℓ3 = S1 { 2 }#0; run drop fn S1(S1 { 2 }#0); drop ℓ2 = S5 { 1, ⊘ }#1; run drop fn S5(S5 { 1, ⊘ }#1)

