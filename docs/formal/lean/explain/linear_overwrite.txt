════════════════════════════════════════════════════════════════════════════════
 linear_overwrite
════════════════════════════════════════════════════════════════════════════════
Assigning over a live linear value: rejected statically (3.8:77, the RUE-387
premise) and refused dynamically (linearOverwrite).

Rules exercised: (Assign) §5.2 · 3.8:77

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let mut v0: S2 = S2 { x0: 1 };
        {
            { v0 = S2 { x0: 2 }; };
            {
                @drop(v0);
                0
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Assign) §5.2, 3.8:77:

    on  { v0 = S2 { x0: 2 }; }

    overwrite of a live linear value: the place is not MovedOut after the
    right-hand side and its type S2 carries a linear value ((Assign) premise
    `Σ1(p) = MovedOut ∨ ¬carries_linear(T)`, §5.2, read on the destination's
    type; 3.8:77; the compiler reports E0493)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let mut v0: S2 = S2 { x0: 1 }; { { v0 = S2 { x0: 2 }; }; { @drop(v0); 0 } } }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S2 { x0: 1 }
        Γ;Σ     (empty)
        ⇒       S2  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { { v0 = S2 { x0: 2 }; }; { @drop(v0); 0 } }
        Γ;Σ     [v0: S2 mut = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Assign) §5.2, 3.8:77
            e       { v0 = S2 { x0: 2 }; }
            Γ;Σ     [v0: S2 mut = Owned]
            ✗       this premise fails:
                    overwrite of a live linear value: the place is not MovedOut after
                    the right-hand side and its type S2 carries a linear value ((Assign)
                    premise `Σ1(p) = MovedOut ∨ ¬carries_linear(T)`, §5.2, read on the
                    destination's type; 3.8:77; the compiler reports E0493)
              (Struct-Intro) §5.8
                e       S2 { x0: 2 }
                Γ;Σ     [v0: S2 mut = Owned]
                ⇒       S2  ⊣  [v0: S2 mut = Owned]
                  (Lit) §5.8
                    e       2
                    Γ;Σ     [v0: S2 mut = Owned]
                    ⇒       i64  ⊣  [v0: S2 mut = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d3        (D-Struct) §6.5 (mint #0)
         expr    S2 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S2 { 1 }#0
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let mut v0 = S2 { 1 }#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0]
         result  value S2 { 1 }#0
  [5]   d6              literal §6.3
         expr    2
         store   [ℓ0 = †, ℓ1 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0]
         result  value 2
  [6]   d5            (D-Struct) §6.5 (mint #2)
         expr    S2 { x0: 2 }
         store   [ℓ0 = †, ℓ1 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0, ℓ2 = †]
         result  value S2 { 2 }#2
  [7]   d4          (D-Assign) §6.8
         expr    { v0 = S2 { x0: 2 }; }
         store   [ℓ0 = †, ℓ1 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0, ℓ2 = †]
         result  REFUSED (linearOverwrite)
                 an overwrite-drop of a live linear value: the assignment would
                 consume a linear value the program never consumed (§6.8; 3.8:77; §7)
  [8]   d3        (D-Seq) §6.7 — a premise did not complete
         expr    { { v0 = S2 { x0: 2 }; }; { @drop(v0); 0 } }
         store   [ℓ0 = †, ℓ1 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0, ℓ2 = †]
         result  REFUSED (linearOverwrite)
                 an overwrite-drop of a live linear value: the assignment would
                 consume a linear value the program never consumed (§6.8; 3.8:77; §7)
  [9]   d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let mut v0: S2 = S2 { x0: 1 }; { { v0 = S2 { x0: 2 }; }; { @drop(v0); 0 } } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0, ℓ2 = †]
         result  REFUSED (linearOverwrite)
                 an overwrite-drop of a live linear value: the assignment would
                 consume a linear value the program never consumed (§6.8; 3.8:77; §7)
  [10]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S2 { 1 }#0, ℓ2 = †]
         result  REFUSED (linearOverwrite)
                 an overwrite-drop of a live linear value: the assignment would
                 consume a linear value the program never consumed (§6.8; 3.8:77; §7)

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity, and the step numbers there are the order `drop_order` fixes;
`[n.k]` is the k-th end of step n, when one step ends several values.

  id    minted  ended                             destructor  ended
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     —                                 —           the run was refused (§6)
  #2    [6]     —                                 —           the run was refused (§6)

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearOverwrite)
         an overwrite-drop of a live linear value: the assignment would
         consume a linear value the program never consumed (§6.8; 3.8:77; §7)

