════════════════════════════════════════════════════════════════════════════════
 loop_break_past_linear
════════════════════════════════════════════════════════════════════════════════
A break past a live linear loop-local: the exit ends the local's scope, and
(Loop-Break)'s discharge of the loop-local bindings finds it still Owned (the
compiler reports E0406). The machine's unwind meets the live linear value:
linearLeak.

Rules exercised: (Loop-Break) §5.7 · (Break) §5.7 · §5.6 residual-linear leak check · (D-Break) §6.10 · 3.8:32

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        loop {
            {
                let v0: S3 = S3 { x0: 1 };
                break
            }
        };
        0
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Loop-Break) §5.7:

    on  loop { { let v0: S3 = S3 { x0: 1 }; break } }

    a `break` leaves a binding the loop body opened Owned at a linear type —
    the exit ends its scope, where §5.6's obligation is discharged
    ((Loop-Break) §5.7, `outside_loop`; 3.8:32; the compiler reports E0406)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Seq) §5.3, 3.8:64
    e       { loop { { let v0: S3 = S3 { x0: 1 }; break } }; 0 }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Loop-Break) §5.7
        e       loop { { let v0: S3 = S3 { x0: 1 }; break } }
        Γ;Σ     (empty)
        ✗       this premise fails:
                a `break` leaves a binding the loop body opened Owned at a linear
                type — the exit ends its scope, where §5.6's obligation is
                discharged ((Loop-Break) §5.7, `outside_loop`; 3.8:32; the compiler
                reports E0406)
          (Let) §5.3, the body diverging
            e       { let v0: S3 = S3 { x0: 1 }; break }
            Γ;Σ     (empty)
            ⇒       never  ⊣  ⊥; Δ = {⟨break, [v0: S3 = Owned]⟩}
              (Struct-Intro) §5.8
                e       S3 { x0: 1 }
                Γ;Σ     (empty)
                ⇒       S3  ⊣  (empty)
                  (Lit) §5.8
                    e       1
                    Γ;Σ     (empty)
                    ⇒       i64  ⊣  (empty)
              (Break) §5.7 + (Sub-Never) §5.7
                e       break
                Γ;Σ     [v0: S3 = Owned]
                ⇒       never  ⊣  ⊥; Δ = {⟨break, [v0: S3 = Owned]⟩}

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d6              literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            (D-Struct) §6.5 (mint #0)
         expr    S3 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S3 { 1 }#0
  [4]   d5            (D-Let) §6.7 (mint the binding)
         expr    let v0 = S3 { 1 }#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  value S3 { 1 }#0
  [5]   d5            (D-Break) §6.10
         expr    break
         store   [ℓ0 = †, ℓ1 = S3 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  BREAK — unwinding to the enclosing loop (§6.10)
  [6]   d4          (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v0: S3 = S3 { x0: 1 }; break }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  BREAK — unwinding to the enclosing loop (§6.10)
  [7]   d3        (D-Break) §6.10 (unwind to the loop)
         expr    loop { { let v0: S3 = S3 { x0: 1 }; break } }
         store   [ℓ0 = †, ℓ1 = S3 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [8]   d2      (D-Seq) §6.7 — a premise did not complete
         expr    { loop { { let v0: S3 = S3 { x0: 1 }; break } }; 0 }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [9]   d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

