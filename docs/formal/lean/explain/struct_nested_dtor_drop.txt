════════════════════════════════════════════════════════════════════════════════
 struct_nested_dtor_drop
════════════════════════════════════════════════════════════════════════════════
A destructor-bearing struct holding a destructor-bearing struct, dropped at
scope exit: the outer destructor runs first, then the fields in declaration
order, so the trace is 1 then 2.

Rules exercised: (Struct-Intro) §5.8 · §6.11 · §5.6 scope exit · 3.9:2

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S5 = S5 { x0: 1, x1: S1 { x0: 2 } };
        9
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S5 = S5 { x0: 1, x1: S1 { x0: 2 } }; 9 }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Struct-Intro) §5.8
        e       S5 { x0: 1, x1: S1 { x0: 2 } }
        Γ;Σ     (empty)
        ⇒       S5  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Lit) §5.8
        e       9
        Γ;Σ     [v0: S5 = Owned]
        ⇒       i64  ⊣  [v0: S5 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d4          (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 2 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 2 }#0
  [5]   d3        (D-Struct) §6.5 (mint #1)
         expr    S5 { x0: 1, x1: S1 { x0: 2 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value S5 { 1, S1 { 2 }#0 }#1
  [6]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S5 { 1, S1 { 2 }#0 }#1 at ℓ2
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
         result  value S5 { 1, S1 { 2 }#0 }#1
  [7]   d3        literal §6.3
         expr    9
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
         result  value 9
  [8]   d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S5 { 1, S1 { 2 }#0 }#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         events  >> drop ℓ2 = S5 { 1, S1 { 2 }#0 }#1; run drop fn S5(S5 { 1, S1 { 2 }#0 }#1); run drop fn S1(S1 { 2 }#0)
         result  value 9
  [9]   d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value 9

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 9
         drop trace: drop ℓ2 = S5 { 1, S1 { 2 }#0 }#1; run drop fn S5(S5 { 1, S1 { 2 }#0 }#1); run drop fn S1(S1 { 2 }#0)

