════════════════════════════════════════════════════════════════════════════════
 float_infinities
════════════════════════════════════════════════════════════════════════════════
The infinities sit at the ends of the ordering and print as inf and -inf.

Rules exercised: (D-Float-Ord) §6.4 · 3.12:22 · 3.12:42

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        { let t2g: bool = { let t3a: f64 = (1.0 / 0.0); let t3b: f64 = 1.0; t3a > t3b }; @dbg(t2g) };
        {
            { let t3g: bool = { let t4a: f64 = ((-1.0) / 0.0); let t4b: f64 = 0.0; t4a < t4b }; @dbg(t3g) };
            {
                { let t4g: f64 = (1.0 / 0.0); @dbg(t4g) };
                {
                    { let t5g: f64 = ((-1.0) / 0.0); @dbg(t5g) };
                    0
                }
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Seq) §5.3, 3.8:64
    e       { @dbg(((1.0 / 0.0) > 1.0)); { @dbg((((-1.0) / 0.0) < 0.0)); { @dbg((1.0 / 0.0)…
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Dbg) §5.8
        e       @dbg(((1.0 / 0.0) > 1.0))
        Γ;Σ     (empty)
        ⇒       ()  ⊣  (empty)
          (Float-Ord) §5.8
            e       ((1.0 / 0.0) > 1.0)
            Γ;Σ     (empty)
            ⇒       bool  ⊣  (empty)
              (Float-Arith) §5.8
                e       (1.0 / 0.0)
                Γ;Σ     (empty)
                ⇒       f64  ⊣  (empty)
                  (Lit) §5.8
                    e       1.0
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
                  (Lit) §5.8
                    e       0.0
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
              (Lit) §5.8
                e       1.0
                Γ;Σ     (empty)
                ⇒       f64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @dbg((((-1.0) / 0.0) < 0.0)); { @dbg((1.0 / 0.0)); { @dbg(((-1.0) / 0.0)); 0 …
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
          (Dbg) §5.8
            e       @dbg((((-1.0) / 0.0) < 0.0))
            Γ;Σ     (empty)
            ⇒       ()  ⊣  (empty)
              (Float-Ord) §5.8
                e       (((-1.0) / 0.0) < 0.0)
                Γ;Σ     (empty)
                ⇒       bool  ⊣  (empty)
                  (Float-Arith) §5.8
                    e       ((-1.0) / 0.0)
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
                      (Float-Neg) §5.8
                        e       (-1.0)
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
                          (Lit) §5.8
                            e       1.0
                            Γ;Σ     (empty)
                            ⇒       f64  ⊣  (empty)
                      (Lit) §5.8
                        e       0.0
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
                  (Lit) §5.8
                    e       0.0
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
          (Seq) §5.3, 3.8:64
            e       { @dbg((1.0 / 0.0)); { @dbg(((-1.0) / 0.0)); 0 } }
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
              (Dbg) §5.8
                e       @dbg((1.0 / 0.0))
                Γ;Σ     (empty)
                ⇒       ()  ⊣  (empty)
                  (Float-Arith) §5.8
                    e       (1.0 / 0.0)
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
                      (Lit) §5.8
                        e       1.0
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
                      (Lit) §5.8
                        e       0.0
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
              (Seq) §5.3, 3.8:64
                e       { @dbg(((-1.0) / 0.0)); 0 }
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
                  (Dbg) §5.8
                    e       @dbg(((-1.0) / 0.0))
                    Γ;Σ     (empty)
                    ⇒       ()  ⊣  (empty)
                      (Float-Arith) §5.8
                        e       ((-1.0) / 0.0)
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
                          (Float-Neg) §5.8
                            e       (-1.0)
                            Γ;Σ     (empty)
                            ⇒       f64  ⊣  (empty)
                              (Lit) §5.8
                                e       1.0
                                Γ;Σ     (empty)
                                ⇒       f64  ⊣  (empty)
                          (Lit) §5.8
                            e       0.0
                            Γ;Σ     (empty)
                            ⇒       f64  ⊣  (empty)
                  (Lit) §5.8
                    e       0
                    Γ;Σ     (empty)
                    ⇒       i64  ⊣  (empty)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d6              literal §6.3 (3.12:9 rounds the decimal)
         expr    1.0
         store   (empty)
           →     (empty)
         result  value 1.0
  [3]   d6              literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [4]   d5            (D-Float-Arith) §6.4
         expr    (1.0 / 0.0)
         store   (empty)
           →     (empty)
         result  value inf
  [5]   d5            literal §6.3 (3.12:9 rounds the decimal)
         expr    1.0
         store   (empty)
           →     (empty)
         result  value 1.0
  [6]   d4          (D-Float-Ord) §6.4
         expr    ((1.0 / 0.0) > 1.0)
         store   (empty)
           →     (empty)
         result  value true
  [7]   d3        (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(((1.0 / 0.0) > 1.0))
         store   (empty)
           →     (empty)
         events  >> @dbg prints true
         result  value ()
  [8]   d8                  literal §6.3 (3.12:9 rounds the decimal)
         expr    1.0
         store   (empty)
           →     (empty)
         result  value 1.0
  [9]   d7                (D-Float-Neg) §6.4
         expr    (-1.0)
         store   (empty)
           →     (empty)
         result  value -1.0
  [10]  d7                literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [11]  d6              (D-Float-Arith) §6.4
         expr    ((-1.0) / 0.0)
         store   (empty)
           →     (empty)
         result  value -inf
  [12]  d6              literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [13]  d5            (D-Float-Ord) §6.4
         expr    (((-1.0) / 0.0) < 0.0)
         store   (empty)
           →     (empty)
         result  value true
  [14]  d4          (Dbg) §5.8, the observable output of §6.12
         expr    @dbg((((-1.0) / 0.0) < 0.0))
         store   (empty)
           →     (empty)
         events  >> @dbg prints true
         result  value ()
  [15]  d7                literal §6.3 (3.12:9 rounds the decimal)
         expr    1.0
         store   (empty)
           →     (empty)
         result  value 1.0
  [16]  d7                literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [17]  d6              (D-Float-Arith) §6.4
         expr    (1.0 / 0.0)
         store   (empty)
           →     (empty)
         result  value inf
  [18]  d5            (Dbg) §5.8, the observable output of §6.12
         expr    @dbg((1.0 / 0.0))
         store   (empty)
           →     (empty)
         events  >> @dbg prints inf
         result  value ()
  [19]  d9                  literal §6.3 (3.12:9 rounds the decimal)
         expr    1.0
         store   (empty)
           →     (empty)
         result  value 1.0
  [20]  d8                  (D-Float-Neg) §6.4
         expr    (-1.0)
         store   (empty)
           →     (empty)
         result  value -1.0
  [21]  d8                  literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [22]  d7                (D-Float-Arith) §6.4
         expr    ((-1.0) / 0.0)
         store   (empty)
           →     (empty)
         result  value -inf
  [23]  d6              (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(((-1.0) / 0.0))
         store   (empty)
           →     (empty)
         events  >> @dbg prints -inf
         result  value ()
  [24]  d6              literal §6.3
         expr    0
         store   (empty)
           →     (empty)
         result  value 0
  [25]  d5            (D-Seq) §6.7
         expr    { @dbg(((-1.0) / 0.0)); 0 }
         store   (empty)
           →     (empty)
         result  value 0
  [26]  d4          (D-Seq) §6.7
         expr    { @dbg((1.0 / 0.0)); { @dbg(((-1.0) / 0.0)); 0 } }
         store   (empty)
           →     (empty)
         result  value 0
  [27]  d3        (D-Seq) §6.7
         expr    { @dbg((((-1.0) / 0.0) < 0.0)); { @dbg((1.0 / 0.0)); { @dbg(((-1.0) / 0.0)); 0 …
         store   (empty)
           →     (empty)
         result  value 0
  [28]  d2      (D-Seq) §6.7
         expr    { @dbg(((1.0 / 0.0) > 1.0)); { @dbg((((-1.0) / 0.0) < 0.0)); { @dbg((1.0 / 0.0)…
         store   (empty)
           →     (empty)
         result  value 0
  [29]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   (empty)
           →     (empty)
         result  value 0

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 0
         drop trace: @dbg prints true; @dbg prints true; @dbg prints inf; @dbg prints -inf

