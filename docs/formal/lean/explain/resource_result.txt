════════════════════════════════════════════════════════════════════════════════
 resource_result
════════════════════════════════════════════════════════════════════════════════
The program's value is a resource: main observes its payload and never drops
it.

Rules exercised: §4.3 expression value

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> RAffine {
    {
        let v0: i64 = 4;
        RAffine { value: v0, live: true }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> RAffine  — body ⇒ RAffine, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> RAffine

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: i64 = 4; RAffine { value: v0, live: true } }
    Γ;Σ     (empty)
    ⇒       RAffine  ⊣  (empty)
      (Lit) §5.8
        e       4
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
      resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
        e       RAffine { value: v0, live: true }
        Γ;Σ     [v0: i64 = Owned]
        ⇒       RAffine  ⊣  [v0: i64 = Owned]
          (Use-Copy) §5.1
            e       v0
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> RAffine  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d3        literal §6.3
         expr    4
         store   (empty)
           →     (empty)
         result  value 4
  [3]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = 4 at ℓ0
         store   (empty)
           →     [ℓ0 = 4]
         result  value 4
  [4]   d4          (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = 4]
           →     [ℓ0 = 4]
         result  value 4
  [5]   d3        resource intro §6.5
         expr    RAffine { value: v0, live: true }
         store   [ℓ0 = 4]
           →     [ℓ0 = 4]
         result  value RAffine { 4 }
  [6]   d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = 4]
           →     [ℓ0 = † (retired)]
         result  value RAffine { 4 }
  [7]   d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = † (retired)]
           →     [ℓ0 = † (retired)]
         result  value RAffine { 4 }

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value RAffine { 4 }
         drop trace: (no drops)

