════════════════════════════════════════════════════════════════════════════════
 destructure_under_dtor
════════════════════════════════════════════════════════════════════════════════
A destructure out of a value whose type declares a destructor: 3.9:34 forbids
it at every enclosing value, d included (E0456), and no monitor enforces it,
so the machine runs the program.

Rules exercised: (Use-Declared-Linear-Destructure) §5.1 · 3.9:34

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
linear struct S11 { x0: i64, x1: S1 }
struct S12 { x0: S11, x1: S1 }
linear struct S13 { x0: S11, x1: S1 }
linear struct S14 { x0: S1, x1: S1 }
linear struct S15 { x0: S1, x1: i64, x2: S1 }
linear struct S16 { x0: S8, x1: S1 }
linear struct S17 { x0: i64, x1: S2 }
linear struct S18 { x0: i64, x1: S1 }
drop fn S18(self) { @dbg(self.x0); }
struct S19 { x0: i64, x1: S2 }
linear struct S20 { x0: S19, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S18 = S18 { x0: 1, x1: S1 { x0: 2 } };
        {
            let v1: i64 = v0.x0;
            v1
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Use-Declared-Linear-Destructure) §5.1:

    on  v0.x0

    a proper prefix of the path has a type that declares a destructor, so the
    field may not be moved out of it — the destructor runs on the whole value
    and would observe the hole ((Use-Move)/(@Drop) premise, §5.1, §5.3;
    3.9:34; the compiler reports E0456)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S18 = S18 { x0: 1, x1: S1 { x0: 2 } }; { let v1: i64 = v0.x0; v1 } }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S18 { x0: 1, x1: S1 { x0: 2 } }
        Γ;Σ     (empty)
        ⇒       S18  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: i64 = v0.x0; v1 }
        Γ;Σ     [v0: S18 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Use-Declared-Linear-Destructure) §5.1
            e       v0.x0
            Γ;Σ     [v0: S18 = Owned]
            ✗       this premise fails:
                    a proper prefix of the path has a type that declares a destructor,
                    so the field may not be moved out of it — the destructor runs on the
                    whole value and would observe the hole ((Use-Move)/(@Drop) premise,
                    §5.1, §5.3; 3.9:34; the compiler reports E0456)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d4          (D-Struct) §6.5
         expr    S1 { x0: 2 }
         store   (empty)
           →     (empty)
         result  value S1 { 2 }
  [5]   d3        (D-Struct) §6.5
         expr    S18 { x0: 1, x1: S1 { x0: 2 } }
         store   (empty)
           →     (empty)
         result  value S18 { 1, S1 { 2 } }
  [6]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S18 { 1, S1 { 2 } } at ℓ0
         store   (empty)
           →     [ℓ0 = S18 { 1, S1 { 2 } }]
         result  value S18 { 1, S1 { 2 } }
  [7]   d4          (D-Use-Declared-Linear) §6.3
         expr    v0.x0
         store   [ℓ0 = S18 { 1, S1 { 2 } }]
           →     [ℓ0 = ⊘]
         events  >> run drop fn S1(S1 { 2 })
         result  value 1
  [8]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = 1 at ℓ1
         store   [ℓ0 = ⊘]
           →     [ℓ0 = ⊘, ℓ1 = 1]
         result  value 1
  [9]   d4          (D-Use-Copy) §6.3
         expr    v1
         store   [ℓ0 = ⊘, ℓ1 = 1]
           →     [ℓ0 = ⊘, ℓ1 = 1]
         result  value 1
  [10]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ1])
         store   [ℓ0 = ⊘, ℓ1 = 1]
           →     [ℓ0 = ⊘, ℓ1 = † (retired)]
         result  value 1
  [11]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = ⊘, ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 1
  [12]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = † (retired), ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 1

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 1
         drop trace: run drop fn S1(S1 { 2 })

