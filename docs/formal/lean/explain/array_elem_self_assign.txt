════════════════════════════════════════════════════════════════════════════════
 array_elem_self_assign
════════════════════════════════════════════════════════════════════════════════
a[0] = a[0] on an [S1; 2]: (Assign) §5.2 evaluates the right-hand side first,
which moves a[0] out, and the write then goes into an array with a moved-out
element, which 3.8:72 and 7.1:46 forbid (E0480). The bridge is red on this
one: the compiler accepts it on purpose (RUE-228 made element self-assignment
reinitialise the element) and prints 100, 1, 2. Which reading is right is
RUE-2346. The case stays seeded until that is decided.

Rules exercised: (Assign) §5.2 · (Use-Move) §5.1 · 3.8:72 · 7.1:46 · 5.2:14

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let mut v0: [S1; 2] = [S1 { x0: 1 }, S1 { x0: 2 }];
        {
            { v0[0] = v0[0]; };
            {
                { let t4g: i64 = 100; @dbg(t4g) };
                0
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Assign) §5.2, 3.8:72, 7.1:46:

    on  { v0[0] = v0[0]; }

    the destination writes into an array one of whose elements has been moved
    out, and `3.8:72` forbids that "to an element, or through an element"
    alike — an element write does not reinstate per-element ownership
    (`7.1:46`), so the whole array must be reinitialized instead; the compiler
    reports E0480

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let mut v0: [S1; 2] = [S1 { x0: 1 }, S1 { x0: 2 }]; { { v0[0] = v0[0]; }; { @…
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Array-Intro) §5.8
        e       [S1 { x0: 1 }, S1 { x0: 2 }]
        Γ;Σ     (empty)
        ⇒       [S1; 2]  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 1 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { { v0[0] = v0[0]; }; { @dbg(100); 0 } }
        Γ;Σ     [v0: [S1; 2] mut = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Assign) §5.2, 3.8:72, 7.1:46
            e       { v0[0] = v0[0]; }
            Γ;Σ     [v0: [S1; 2] mut = Owned]
            ✗       this premise fails:
                    the destination writes into an array one of whose elements has been
                    moved out, and `3.8:72` forbids that "to an element, or through an
                    element" alike — an element write does not reinstate per-element
                    ownership (`7.1:46`), so the whole array must be reinitialized
                    instead; the compiler reports E0480
              (Use-Move) §5.1
                e       v0[0]
                Γ;Σ     [v0: [S1; 2] mut = Owned]
                ⇒       S1  ⊣  [v0: [S1; 2] mut = Owned{ x0: MovedOut }]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d4          (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 1 }#0
  [4]   d5            literal §6.3
         expr    2
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value 2
  [5]   d4          (D-Struct) §6.5 (mint #1)
         expr    S1 { x0: 2 }
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = †]
         result  value S1 { 2 }#1
  [6]   d3        (D-Array) §6.5 (mint #2)
         expr    [S1 { x0: 1 }, S1 { x0: 2 }]
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value [S1 { 1 }#0, S1 { 2 }#1]#2
  [7]   d3        (D-Let) §6.7 (mint the binding)
         expr    let mut v0 = [S1 { 1 }#0, S1 { 2 }#1]#2 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value [S1 { 1 }#0, S1 { 2 }#1]#2
  [8]   d5            (D-Use-Move) §6.3
         expr    v0[0]
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S1 { 2 }#1]#2]
         result  value S1 { 1 }#0
  [9]   d4          (D-Assign) §6.8 (reinitialization, 3.8:55)
         expr    { v0[0] = v0[0]; }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value ()
  [10]  d6              literal §6.3
         expr    100
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value 100
  [11]  d5            (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(100)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         events  >> @dbg prints 100
         result  value ()
  [12]  d5            literal §6.3
         expr    0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value 0
  [13]  d4          (D-Seq) §6.7
         expr    { @dbg(100); 0 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value 0
  [14]  d3        (D-Seq) §6.7
         expr    { { v0[0] = v0[0]; }; { @dbg(100); 0 } }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
         result  value 0
  [15]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ3])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         events  >> drop ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2; run drop fn S1(S1 { 1 }#0); run drop fn S1(S1 { 2 }#1)
         result  value 0
  [16]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 0

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 0
         drop trace: @dbg prints 100; drop ℓ3 = [S1 { 1 }#0, S1 { 2 }#1]#2; run drop fn S1(S1 { 1 }#0); run drop fn S1(S1 { 2 }#1)

