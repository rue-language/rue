════════════════════════════════════════════════════════════════════════════════
 array_dyn_write_trap
════════════════════════════════════════════════════════════════════════════════
A write at a dynamic index, then the same write at -1: a negative index is out
of range exactly as an oversized one is, so the second call takes
(D-Index-Trap) §6.5's bounds trap and the process exits 101 after printing the
10.

Rules exercised: (Assign) §5.2 · (D-Assign) §6.8 · (D-Index-Trap) §6.5 · §6.12 · 7.1:11 · 4.11:9

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        { let t2g: i64 = f1(1); @dbg(t2g) };
        f1((-1))
    }
}

fn f1(v0: i64) -> i64 {
    {
        let mut v1: [i64; 2] = [1, 2];
        {
            { v1[{ let t3i0: i64 = v0; t3i0 }] = 9; };
            (v1[0] + v1[1])
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)
         fn f1(v0: i64) -> i64  — body ⇒ i64, exit Σ [v0: i64 = Owned]

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Seq) §5.3, 3.8:64
    e       { @dbg(f1(1)); f1((-1)) }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Dbg) §5.8
        e       @dbg(f1(1))
        Γ;Σ     (empty)
        ⇒       ()  ⊣  (empty)
          (Call) §5.8
            e       f1(1)
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Call) §5.8
        e       f1((-1))
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
          (Lit) §5.8
            e       (-1)
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)

  fn f1(v0: i64) -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let mut v1: [i64; 2] = [1, 2]; { { v1[v0] = 9; }; (v1[0] + v1[1]) } }
    Γ;Σ     [v0: i64 = Owned]
    ⇒       i64  ⊣  [v0: i64 = Owned]
      (Array-Intro) §5.8
        e       [1, 2]
        Γ;Σ     [v0: i64 = Owned]
        ⇒       [i64; 2]  ⊣  [v0: i64 = Owned]
          (Lit) §5.8
            e       1
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]
          (Lit) §5.8
            e       2
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]
      (Seq) §5.3, 3.8:64
        e       { { v1[v0] = 9; }; (v1[0] + v1[1]) }
        Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
        ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
          (Assign) §5.2 below a dynamic index
            e       { v1[v0] = 9; }
            Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
            ⇒       ()  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
              (Lit) §5.8
                e       9
                Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
                ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
              (Use-Copy) §5.1
                e       v0
                Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
                ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
          (Arith) §5.8
            e       (v1[0] + v1[1])
            Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
            ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
              (Use-Copy) §5.1
                e       v1[0]
                Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
                ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
              (Use-Copy) §5.1
                e       v1[1]
                Γ;Σ     [v1: [i64; 2] mut = Owned, v0: i64 = Owned]
                ⇒       i64  ⊣  [v1: [i64; 2] mut = Owned, v0: i64 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            (D-Call) §6.9 (push the frame)
         expr    fn f1(v0: i64) -> i64  with [ℓ0]
         store   (empty)
           →     [ℓ0 = 1]
         result  value ()
  [4]   d8                  literal §6.3
         expr    1
         store   [ℓ0 = 1]
           →     [ℓ0 = 1]
         result  value 1
  [5]   d8                  literal §6.3
         expr    2
         store   [ℓ0 = 1]
           →     [ℓ0 = 1]
         result  value 2
  [6]   d7                (D-Array) §6.5
         expr    [1, 2]
         store   [ℓ0 = 1]
           →     [ℓ0 = 1]
         result  value [1, 2]
  [7]   d7                (D-Let) §6.7 (mint the binding)
         expr    let mut v1 = [1, 2] at ℓ1
         store   [ℓ0 = 1]
           →     [ℓ0 = 1, ℓ1 = [1, 2]]
         result  value [1, 2]
  [8]   d9                  literal §6.3
         expr    9
         store   [ℓ0 = 1, ℓ1 = [1, 2]]
           →     [ℓ0 = 1, ℓ1 = [1, 2]]
         result  value 9
  [9]   d9                  (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = 1, ℓ1 = [1, 2]]
           →     [ℓ0 = 1, ℓ1 = [1, 2]]
         result  value 1
  [10]  d8                  (D-Assign) §6.8 below a dynamic index (overwrite-drop)
         expr    { v1[v0] = 9; }
         store   [ℓ0 = 1, ℓ1 = [1, 2]]
           →     [ℓ0 = 1, ℓ1 = [1, 9]]
         result  value ()
  [11]  d9                  (D-Use-Copy) §6.3
         expr    v1[0]
         store   [ℓ0 = 1, ℓ1 = [1, 9]]
           →     [ℓ0 = 1, ℓ1 = [1, 9]]
         result  value 1
  [12]  d9                  (D-Use-Copy) §6.3
         expr    v1[1]
         store   [ℓ0 = 1, ℓ1 = [1, 9]]
           →     [ℓ0 = 1, ℓ1 = [1, 9]]
         result  value 9
  [13]  d8                  (D-Arith) §6.4
         expr    (v1[0] + v1[1])
         store   [ℓ0 = 1, ℓ1 = [1, 9]]
           →     [ℓ0 = 1, ℓ1 = [1, 9]]
         result  value 10
  [14]  d7                (D-Seq) §6.7
         expr    { { v1[v0] = 9; }; (v1[0] + v1[1]) }
         store   [ℓ0 = 1, ℓ1 = [1, 2]]
           →     [ℓ0 = 1, ℓ1 = [1, 9]]
         result  value 10
  [15]  d6              (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ1])
         store   [ℓ0 = 1, ℓ1 = [1, 9]]
           →     [ℓ0 = 1, ℓ1 = † (retired)]
         result  value 10
  [16]  d4          (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([ℓ0])
         store   [ℓ0 = 1, ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 10
  [17]  d3        (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(f1(1))
         store   (empty)
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         events  >> @dbg prints 10
         result  value ()
  [18]  d4          literal §6.3
         expr    (-1)
         store   [ℓ0 = † (retired), ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value -1
  [19]  d4          (D-Call) §6.9 (push the frame)
         expr    fn f1(v0: i64) -> i64  with [ℓ2]
         store   [ℓ0 = † (retired), ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
         result  value ()
  [20]  d7                literal §6.3
         expr    1
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
         result  value 1
  [21]  d7                literal §6.3
         expr    2
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
         result  value 2
  [22]  d6              (D-Array) §6.5
         expr    [1, 2]
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
         result  value [1, 2]
  [23]  d6              (D-Let) §6.7 (mint the binding)
         expr    let mut v1 = [1, 2] at ℓ3
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  value [1, 2]
  [24]  d8                  literal §6.3
         expr    9
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  value 9
  [25]  d8                  (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  value -1
  [26]  d7                (D-Index-Trap) §6.5 — the bounds trap of §6.12
         expr    { v1[v0] = 9; }
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12
  [27]  d6              (D-Seq) §6.7 — a premise did not complete
         expr    { { v1[v0] = 9; }; (v1[0] + v1[1]) }
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12
  [28]  d5            (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let mut v1: [i64; 2] = [1, 2]; { { v1[v0] = 9; }; (v1[0] + v1[1]) } }
         store   [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12
  [29]  d3        (Panic-Lift) §6.2 — the callee trapped, so no frame is popped: §6.12 abandons the configuration and `run-all-scope-drops` never runs
         expr    f1((-1))
         store   [ℓ0 = † (retired), ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12
  [30]  d2      (D-Seq) §6.7
         expr    { @dbg(f1(1)); f1((-1)) }
         store   (empty)
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12
  [31]  d0  (Panic-Lift) §6.2 — the callee trapped, so no frame is popped: §6.12 abandons the configuration and `run-all-scope-drops` never runs
         expr    f0()
         store   (empty)
           →     [ℓ0 = † (retired), ℓ1 = † (retired), ℓ2 = -1, ℓ3 = [1, 2]]
         result  PANIC (bounds) — a defined trap, §6.12

────────────────────────────────────────────────────────────────────────────────
Outcome: PANIC (bounds) — a defined trap, §6.12
         trace up to the trap: @dbg prints 10

