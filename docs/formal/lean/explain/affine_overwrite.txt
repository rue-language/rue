════════════════════════════════════════════════════════════════════════════════
 affine_overwrite
════════════════════════════════════════════════════════════════════════════════
Assigning over a live affine value drops the old value at the assignment, then
the new one at scope exit.

Rules exercised: (Assign) §5.2 · §6.8 overwrite-drop · 3.9:18

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        let mut v0: RAffine = RAffine { value: 1, live: true };
        {
            { v0 = RAffine { value: 2, live: true }; };
            9
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let mut v0: RAffine = RAffine { value: 1, live: true }; { { v0 = RAffine { va…
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
        e       RAffine { value: 1, live: true }
        Γ;Σ     (empty)
        ⇒       RAffine  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { { v0 = RAffine { value: 2, live: true }; }; 9 }
        Γ;Σ     [v0: RAffine mut = Owned]
        ⇒       i64  ⊣  [v0: RAffine mut = Owned]
          (Assign) §5.2, 3.8:77
            e       { v0 = RAffine { value: 2, live: true }; }
            Γ;Σ     [v0: RAffine mut = Owned]
            ⇒       ()  ⊣  [v0: RAffine mut = Owned]
              resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
                e       RAffine { value: 2, live: true }
                Γ;Σ     [v0: RAffine mut = Owned]
                ⇒       RAffine  ⊣  [v0: RAffine mut = Owned]
                  (Lit) §5.8
                    e       2
                    Γ;Σ     [v0: RAffine mut = Owned]
                    ⇒       i64  ⊣  [v0: RAffine mut = Owned]
          (Lit) §5.8
            e       9
            Γ;Σ     [v0: RAffine mut = Owned]
            ⇒       i64  ⊣  [v0: RAffine mut = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d3        resource intro §6.5
         expr    RAffine { value: 1, live: true }
         store   (empty)
           →     (empty)
         result  value RAffine { 1 }
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let mut v0 = RAffine { 1 } at ℓ0
         store   (empty)
           →     [ℓ0 = RAffine { 1 }]
         result  value RAffine { 1 }
  [5]   d6              literal §6.3
         expr    2
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = RAffine { 1 }]
         result  value 2
  [6]   d5            resource intro §6.5
         expr    RAffine { value: 2, live: true }
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = RAffine { 1 }]
         result  value RAffine { 2 }
  [7]   d4          (D-Assign) §6.8 (overwrite-drop)
         expr    { v0 = RAffine { value: 2, live: true }; }
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = RAffine { 2 }]
         events  >> drop ℓ0 = RAffine { 1 }
         result  value ()
  [8]   d4          literal §6.3
         expr    9
         store   [ℓ0 = RAffine { 2 }]
           →     [ℓ0 = RAffine { 2 }]
         result  value 9
  [9]   d3        (D-Seq) §6.7
         expr    { { v0 = RAffine { value: 2, live: true }; }; 9 }
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = RAffine { 2 }]
         result  value 9
  [10]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = RAffine { 2 }]
           →     [ℓ0 = † (retired)]
         events  >> drop ℓ0 = RAffine { 2 }
         result  value 9
  [11]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = † (retired)]
           →     [ℓ0 = † (retired)]
         result  value 9

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 9
         drop trace: drop ℓ0 = RAffine { 1 }; drop ℓ0 = RAffine { 2 }

