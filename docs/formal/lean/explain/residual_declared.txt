════════════════════════════════════════════════════════════════════════════════
 residual_declared
════════════════════════════════════════════════════════════════════════════════
Destructuring the innermost declared-linear level of a two-level
declared-linear struct (y.x0.x0) leaves the outer struct y Owned: §5.6's
residual-linear check reads the declared-linear attribute itself, whatever the
fields hold (3.8:74), and refuses the leak (the compiler: E0406). The
machine's monitor still refuses it dynamically with linearLeak. Seeded by the
definition mutation analysis (RUE-2486): mutant `residual-declared`.

Rules exercised: §5.6 residual-linear leak check · 3.8:74

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
linear struct S11 { x0: i64, x1: S1 }
struct S12 { x0: S11, x1: S1 }
linear struct S13 { x0: S11, x1: S1 }
linear struct S14 { x0: S1, x1: S1 }
linear struct S15 { x0: S1, x1: i64, x2: S1 }
linear struct S16 { x0: S8, x1: S1 }
linear struct S17 { x0: i64, x1: S2 }
linear struct S18 { x0: i64, x1: S1 }
drop fn S18(self) { @dbg(self.x0); }
struct S19 { x0: i64, x1: S2 }
linear struct S20 { x0: S19, x1: S1 }
linear struct S21 { x0: [S1; 2], x1: i64 }
linear struct S22 { x0: [S1; 2], x1: S1 }
fn f0() -> i64 {
    {
        let v0: S13 = S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3 } };
        {
            let v1: i64 = v0.x0.x0;
            v1
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Let) §5.3 + the §5.6 scope-exit leak check:

    on  { let v0: S13 = S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3…

    the residual state of the `let` binder still carries a linear value at
    type S13 — a linear value reached end of scope unconsumed (§5.6's
    `residual-linear(Σ, x, T)`, read on the residue after any partial move;
    3.8:32; the compiler reports E0406)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S13 = S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3 } }; …
    Γ;Σ     (empty)
    ✗       this premise fails:
            the residual state of the `let` binder still carries a linear value
            at type S13 — a linear value reached end of scope unconsumed (§5.6's
            `residual-linear(Σ, x, T)`, read on the residue after any partial
            move; 3.8:32; the compiler reports E0406)
      (Struct-Intro) §5.8
        e       S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3 } }
        Γ;Σ     (empty)
        ⇒       S13  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S11 { x0: 1, x1: S1 { x0: 2 } }
            Γ;Σ     (empty)
            ⇒       S11  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
              (Struct-Intro) §5.8
                e       S1 { x0: 2 }
                Γ;Σ     (empty)
                ⇒       S1  ⊣  (empty)
                  (Lit) §5.8
                    e       2
                    Γ;Σ     (empty)
                    ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 3 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       3
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: i64 = v0.x0.x0; v1 }
        Γ;Σ     [v0: S13 = Owned]
        ⇒       i64  ⊣  [v0: S13 = Owned{ x0: MovedOut }]
          (Use-Declared-Linear-Destructure) §5.1
            e       v0.x0.x0
            Γ;Σ     [v0: S13 = Owned]
            ⇒       i64  ⊣  [v0: S13 = Owned{ x0: MovedOut }]
          (Use-Copy) §5.1
            e       v1
            Γ;Σ     [v1: i64 = Owned, v0: S13 = Owned{ x0: MovedOut }]
            ⇒       i64  ⊣  [v1: i64 = Owned, v0: S13 = Owned{ x0: MovedOut }]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d6              literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d5            (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 2 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 2 }#0
  [5]   d4          (D-Struct) §6.5 (mint #1)
         expr    S11 { x0: 1, x1: S1 { x0: 2 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value S11 { 1, S1 { 2 }#0 }#1
  [6]   d5            literal §6.3
         expr    3
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †]
         result  value 3
  [7]   d4          (D-Struct) §6.5 (mint #2)
         expr    S1 { x0: 3 }
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value S1 { 3 }#2
  [8]   d3        (D-Struct) §6.5 (mint #3)
         expr    S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value S13 { S11 { 1, S1 { 2 }#0 }#1, S1 { 3 }#2 }#3
  [9]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S13 { S11 { 1, S1 { 2 }#0 }#1, S1 { 3 }#2 }#3 at ℓ4
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { S11 { 1, S1 { 2 }#0 }#1, S1…
         result  value S13 { S11 { 1, S1 { 2 }#0 }#1, S1 { 3 }#2 }#3
  [10]  d4          (D-Use-Declared-Linear) §6.3
         expr    v0.x0.x0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { S11 { 1, S1 { 2 }#0 }#1, S1…
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3]
         events  >> drop ℓ4 = S1 { 2 }#0; run drop fn S1(S1 { 2 }#0); consume S11 { ⊘, ⊘ }#1
         result  value 1
  [11]  d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = 1 at ℓ5
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = 1]
         result  value 1
  [12]  d4          (D-Use-Copy) §6.3
         expr    v1
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = 1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = 1]
         result  value 1
  [13]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ5])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = 1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = †]
         result  value 1
  [14]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    { let v0: S13 = S13 { x0: S11 { x0: 1, x1: S1 { x0: 2 } }, x1: S1 { x0: 3 } }; …
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = †]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [15]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †, ℓ4 = S13 { ⊘, S1 { 3 }#2 }#3, ℓ5 = †]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [4]     [10.1] drop ℓ4                    [10]        once
  #1    [5]     [10.2] consume                    —           once
  #2    [7]     —                                 —           the run was refused (§6)
  #3    [8]     —                                 —           the run was refused (§6)

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

