════════════════════════════════════════════════════════════════════════════════
 residual_untracked
════════════════════════════════════════════════════════════════════════════════
A two-element linear array with only the first element moved out: §5.6's
residual-linear check reads the untracked second element at the type level
(the second disjunct, an untouched slot carries linear whenever its declared
type does), and refuses the leak (the compiler: E0406) even though no tracked
state marks that element Owned. The machine's monitor refuses it dynamically
with linearLeak. Seeded by the definition mutation analysis (RUE-2486): mutant
`residual-untracked`.

Rules exercised: §5.6 residual-linear leak check

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: [S2; 2] = [S2 { x0: 1 }, S2 { x0: 2 }];
        {
            let v1: S2 = v0[0];
            {
                @drop(v1);
                7
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Let) §5.3 + the §5.6 scope-exit leak check:

    on  { let v0: [S2; 2] = [S2 { x0: 1 }, S2 { x0: 2 }]; { let v1: S2 = v0[0]; {…

    the residual state of the `let` binder still carries a linear value at
    type [S2; 2] — a linear value reached end of scope unconsumed (§5.6's
    `residual-linear(Σ, x, T)`, read on the residue after any partial move;
    3.8:32; the compiler reports E0406)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: [S2; 2] = [S2 { x0: 1 }, S2 { x0: 2 }]; { let v1: S2 = v0[0]; { @drop…
    Γ;Σ     (empty)
    ✗       this premise fails:
            the residual state of the `let` binder still carries a linear value
            at type [S2; 2] — a linear value reached end of scope unconsumed
            (§5.6's `residual-linear(Σ, x, T)`, read on the residue after any
            partial move; 3.8:32; the compiler reports E0406)
      (Array-Intro) §5.8
        e       [S2 { x0: 1 }, S2 { x0: 2 }]
        Γ;Σ     (empty)
        ⇒       [S2; 2]  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S2 { x0: 1 }
            Γ;Σ     (empty)
            ⇒       S2  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S2 { x0: 2 }
            Γ;Σ     (empty)
            ⇒       S2  ⊣  (empty)
              (Lit) §5.8
                e       2
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: S2 = v0[0]; { @drop(v1); 7 } }
        Γ;Σ     [v0: [S2; 2] = Owned]
        ⇒       i64  ⊣  [v0: [S2; 2] = Owned{ x0: MovedOut }]
          (Use-Move) §5.1
            e       v0[0]
            Γ;Σ     [v0: [S2; 2] = Owned]
            ⇒       S2  ⊣  [v0: [S2; 2] = Owned{ x0: MovedOut }]
          (Seq) §5.3, 3.8:64
            e       { @drop(v1); 7 }
            Γ;Σ     [v1: S2 = Owned, v0: [S2; 2] = Owned{ x0: MovedOut }]
            ⇒       i64  ⊣  [v1: S2 = MovedOut, v0: [S2; 2] = Owned{ x0: MovedOut }]
              (@Drop) §5.3
                e       @drop(v1)
                Γ;Σ     [v1: S2 = Owned, v0: [S2; 2] = Owned{ x0: MovedOut }]
                ⇒       ()  ⊣  [v1: S2 = MovedOut, v0: [S2; 2] = Owned{ x0: MovedOut }]
              (Lit) §5.8
                e       7
                Γ;Σ     [v1: S2 = MovedOut, v0: [S2; 2] = Owned{ x0: MovedOut }]
                ⇒       i64  ⊣  [v1: S2 = MovedOut, v0: [S2; 2] = Owned{ x0: MovedOut }]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d4          (D-Struct) §6.5 (mint #0)
         expr    S2 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S2 { 1 }#0
  [4]   d5            literal §6.3
         expr    2
         store   [ℓ0 = †]
           →     [ℓ0 = †]
         result  value 2
  [5]   d4          (D-Struct) §6.5 (mint #1)
         expr    S2 { x0: 2 }
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = †]
         result  value S2 { 2 }#1
  [6]   d3        (D-Array) §6.5 (mint #2)
         expr    [S2 { x0: 1 }, S2 { x0: 2 }]
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value [S2 { 1 }#0, S2 { 2 }#1]#2
  [7]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = [S2 { 1 }#0, S2 { 2 }#1]#2 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S2 { 1 }#0, S2 { 2 }#1]#2]
         result  value [S2 { 1 }#0, S2 { 2 }#1]#2
  [8]   d4          (D-Use-Move) §6.3
         expr    v0[0]
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [S2 { 1 }#0, S2 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2]
         result  value S2 { 1 }#0
  [9]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = S2 { 1 }#0 at ℓ4
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = S2 { 1 }#0]
         result  value S2 { 1 }#0
  [10]  d5            @drop §6.11
         expr    @drop(v1)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = ⊘]
         events  >> drop ℓ4 = S2 { 1 }#0
         result  value ()
  [11]  d5            literal §6.3
         expr    7
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = ⊘]
         result  value 7
  [12]  d4          (D-Seq) §6.7
         expr    { @drop(v1); 7 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = S2 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = ⊘]
         result  value 7
  [13]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ4])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = †]
         result  value 7
  [14]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    { let v0: [S2; 2] = [S2 { x0: 1 }, S2 { x0: 2 }]; { let v1: S2 = v0[0]; { @drop…
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = †]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [15]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = [⊘, S2 { 2 }#1]#2, ℓ4 = †]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     [10] drop ℓ4                      —           once
  #1    [5]     —                                 —           the run was refused (§6)
  #2    [6]     —                                 —           the run was refused (§6)

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

