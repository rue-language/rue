════════════════════════════════════════════════════════════════════════════════
 enum_temporary_scrutinee
════════════════════════════════════════════════════════════════════════════════
A temporary scrutinee: the enum is built in scrutinee position and never
bound, so the arm's payload binding is the only owner there is.

Rules exercised: (Match) §5.5 · (Enum-Intro) §5.5 · (D-Match) §6.6 · 6.3:17

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
struct S11 { x0: E0, x1: S1 }
enum E0 { K0(S1), K1 }
enum E1 { K0(S3), K1 }
enum E2 { K0(S1, S1), K1 }
enum E3 { K0, K1 }
enum E4 { K0(i64), K1 }
enum E5 { K0(S1, S3), K1 }
fn f0() -> i64 {
    {
        let v0: i64 = match E0.K0(S1 { x0: 1 }) {
            E0.K0(v0) => {
                {
                    { let t5g: i64 = 10; @dbg(t5g) };
                    5
                }
            },
            E0.K1 => {
                6
            },
        };
        {
            { let t3g: i64 = 20; @dbg(t3g) };
            v0
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: i64 = match E0.K0(S1 { x0: 1 }) { E0.K0(v0) => { @dbg(10); 5 }, E0.K1…
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Match) §5.5 join
        e       match E0.K0(S1 { x0: 1 }) { E0.K0(v0) => { @dbg(10); 5 }, E0.K1 => 6 }
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
          (Enum-Intro) §5.5
            e       E0.K0(S1 { x0: 1 })
            Γ;Σ     (empty)
            ⇒       E0  ⊣  (empty)
              (Struct-Intro) §5.8
                e       S1 { x0: 1 }
                Γ;Σ     (empty)
                ⇒       S1  ⊣  (empty)
                  (Lit) §5.8
                    e       1
                    Γ;Σ     (empty)
                    ⇒       i64  ⊣  (empty)
          (Seq) §5.3, 3.8:64
            e       { @dbg(10); 5 }
            Γ;Σ     [v0: S1 = Owned]
            ⇒       i64  ⊣  [v0: S1 = Owned]
              (Dbg) §5.8
                e       @dbg(10)
                Γ;Σ     [v0: S1 = Owned]
                ⇒       ()  ⊣  [v0: S1 = Owned]
                  (Lit) §5.8
                    e       10
                    Γ;Σ     [v0: S1 = Owned]
                    ⇒       i64  ⊣  [v0: S1 = Owned]
              (Lit) §5.8
                e       5
                Γ;Σ     [v0: S1 = Owned]
                ⇒       i64  ⊣  [v0: S1 = Owned]
          (Lit) §5.8
            e       6
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @dbg(20); v0 }
        Γ;Σ     [v0: i64 = Owned]
        ⇒       i64  ⊣  [v0: i64 = Owned]
          (Dbg) §5.8
            e       @dbg(20)
            Γ;Σ     [v0: i64 = Owned]
            ⇒       ()  ⊣  [v0: i64 = Owned]
              (Lit) §5.8
                e       20
                Γ;Σ     [v0: i64 = Owned]
                ⇒       i64  ⊣  [v0: i64 = Owned]
          (Use-Copy) §5.1
            e       v0
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d6              literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d5            (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 1 }#0
  [4]   d4          (D-Enum-Intro) §6.6 (mint #1)
         expr    E0.K0(S1 { x0: 1 })
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value E0.K0⟨S1 { 1 }#0⟩#1
  [5]   d4          (D-Match) §6.6 (bind the arm's payload)
         expr    bind E0.K0's payload to [ℓ2]
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
         events  >> consume E0.K0⟨⊘⟩#1
         result  value E0.K0⟨S1 { 1 }#0⟩#1
  [6]   d6              literal §6.3
         expr    10
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
         result  value 10
  [7]   d5            (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(10)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
         events  >> @dbg prints 10
         result  value ()
  [8]   d5            literal §6.3
         expr    5
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
         result  value 5
  [9]   d4          (D-Seq) §6.7
         expr    { @dbg(10); 5 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
         result  value 5
  [10]  d3        (D-EndScope) §6.6 (end the arm)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = S1 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         events  >> drop ℓ2 = S1 { 1 }#0; run drop fn S1(S1 { 1 }#0)
         result  value 5
  [11]  d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = 5 at ℓ3
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
         result  value 5
  [12]  d5            literal §6.3
         expr    20
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
         result  value 20
  [13]  d4          (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(20)
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
         events  >> @dbg prints 20
         result  value ()
  [14]  d4          (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
         result  value 5
  [15]  d3        (D-Seq) §6.7
         expr    { @dbg(20); v0 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
         result  value 5
  [16]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ3])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = 5]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 5
  [17]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 5

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     [10] drop ℓ2                      [10]        once
  #1    [4]     [5] consume                       —           once

  Every owned identity is ended exactly once (`drop_exactly_once`).

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 5
         drop trace: consume E0.K0⟨⊘⟩#1; @dbg prints 10; drop ℓ2 = S1 { 1 }#0; run drop fn S1(S1 { 1 }#0); @dbg prints 20

