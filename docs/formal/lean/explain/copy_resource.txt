════════════════════════════════════════════════════════════════════════════════
 copy_resource
════════════════════════════════════════════════════════════════════════════════
A copy struct: @drop is a no-op, uses copy, nothing is ever printed for it.

Rules exercised: (Use-Copy) §5.1 · (@Drop-Copy) §5.3 · 3.9:31

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S0 = S0 { x0: 5 };
        {
            @drop(v0);
            (v0.x0 + v0.x0)
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S0 = S0 { x0: 5 }; { @drop(v0); (v0.x0 + v0.x0) } }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Struct-Intro) §5.8
        e       S0 { x0: 5 }
        Γ;Σ     (empty)
        ⇒       S0  ⊣  (empty)
          (Lit) §5.8
            e       5
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @drop(v0); (v0.x0 + v0.x0) }
        Γ;Σ     [v0: S0 = Owned]
        ⇒       i64  ⊣  [v0: S0 = Owned]
          (@Drop-Copy) §5.3
            e       @drop(v0)
            Γ;Σ     [v0: S0 = Owned]
            ⇒       ()  ⊣  [v0: S0 = Owned]
          (Arith) §5.8
            e       (v0.x0 + v0.x0)
            Γ;Σ     [v0: S0 = Owned]
            ⇒       i64  ⊣  [v0: S0 = Owned]
              (Use-Copy) §5.1
                e       v0.x0
                Γ;Σ     [v0: S0 = Owned]
                ⇒       i64  ⊣  [v0: S0 = Owned]
              (Use-Copy) §5.1
                e       v0.x0
                Γ;Σ     [v0: S0 = Owned]
                ⇒       i64  ⊣  [v0: S0 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    5
         store   (empty)
           →     (empty)
         result  value 5
  [3]   d3        (D-Struct) §6.5
         expr    S0 { x0: 5 }
         store   (empty)
           →     (empty)
         result  value S0 { 5 }
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S0 { 5 } at ℓ0
         store   (empty)
           →     [ℓ0 = S0 { 5 }]
         result  value S0 { 5 }
  [5]   d4          @drop §6.11 (Copy: no glue)
         expr    @drop(v0)
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = S0 { 5 }]
         result  value ()
  [6]   d5            (D-Use-Copy) §6.3
         expr    v0.x0
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = S0 { 5 }]
         result  value 5
  [7]   d5            (D-Use-Copy) §6.3
         expr    v0.x0
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = S0 { 5 }]
         result  value 5
  [8]   d4          (D-Arith) §6.4
         expr    (v0.x0 + v0.x0)
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = S0 { 5 }]
         result  value 10
  [9]   d3        (D-Seq) §6.7
         expr    { @drop(v0); (v0.x0 + v0.x0) }
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = S0 { 5 }]
         result  value 10
  [10]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = S0 { 5 }]
           →     [ℓ0 = † (retired)]
         result  value 10
  [11]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = † (retired)]
           →     [ℓ0 = † (retired)]
         result  value 10

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 10
         drop trace: (no drops)

