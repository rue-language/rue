════════════════════════════════════════════════════════════════════════════════
 copy_resource
════════════════════════════════════════════════════════════════════════════════
A copy resource: @drop is a no-op, uses copy, nothing is ever printed for it.

Rules exercised: (Use-Copy) §5.1 · (@Drop-Copy) §5.3

── The program ─────────────────────────────────────────────────────────────────

    {
        let v0: RCopy = RCopy { value: 5 };
        {
            @drop(v0);
            (consume_copy(v0) + consume_copy(v0))
        }
    }

── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         type i64, outgoing Γ;Σ (empty)

         `check_sound` ties this acceptance to a real derivation, so the §7
         safety theorems apply to this program.

── The derivation (§5) ─────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order.

  (Let) §5.6 + scope-exit leak check
    e       { let v0: RCopy = RCopy { value: 5 }; { @drop(v0); (consume_copy(v0) + consume_…
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Struct-Intro) §5.8
        e       RCopy { value: 5 }
        Γ;Σ     (empty)
        ⇒       RCopy  ⊣  (empty)
          (Lit) §5.8
            e       5
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @drop(v0); (consume_copy(v0) + consume_copy(v0)) }
        Γ;Σ     [v0: RCopy = Owned]
        ⇒       i64  ⊣  [v0: RCopy = Owned]
          (@Drop-Copy) §5.3
            e       @drop(v0)
            Γ;Σ     [v0: RCopy = Owned]
            ⇒       ()  ⊣  [v0: RCopy = Owned]
          (Arith) §5.8
            e       (consume_copy(v0) + consume_copy(v0))
            Γ;Σ     [v0: RCopy = Owned]
            ⇒       i64  ⊣  [v0: RCopy = Owned]
              (Call) §5.8
                e       consume_copy(v0)
                Γ;Σ     [v0: RCopy = Owned]
                ⇒       i64  ⊣  [v0: RCopy = Owned]
                  (Use-Copy) §5.1
                    e       v0
                    Γ;Σ     [v0: RCopy = Owned]
                    ⇒       RCopy  ⊣  [v0: RCopy = Owned]
              (Call) §5.8
                e       consume_copy(v0)
                Γ;Σ     [v0: RCopy = Owned]
                ⇒       i64  ⊣  [v0: RCopy = Owned]
                  (Use-Copy) §5.1
                    e       v0
                    Γ;Σ     [v0: RCopy = Owned]
                    ⇒       RCopy  ⊣  [v0: RCopy = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth and `>>` marks a drop event.

  [1]   d2      literal §6.3
         expr    5
         store   (empty)
           →     (empty)
         result  value 5
  [2]   d1    (D-Struct) §6.5
         expr    RCopy { value: 5 }
         store   (empty)
           →     (empty)
         result  value RCopy { 5 }
  [3]   d1    (D-Let) §6.7 (mint the binding)
         expr    { let v0: RCopy = RCopy { value: 5 }; { @drop(v0); (consume_copy(v0) + consume_…
         store   (empty)
           →     [ℓ0 = RCopy { 5 }]
         result  value RCopy { 5 }
  [4]   d2      @drop §6.11 (Copy: no glue)
         expr    @drop(v0)
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value ()
  [5]   d4          (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value RCopy { 5 }
  [6]   d3        (D-Call) §6.9
         expr    consume_copy(v0)
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value 5
  [7]   d4          (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value RCopy { 5 }
  [8]   d3        (D-Call) §6.9
         expr    consume_copy(v0)
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value 5
  [9]   d2      (D-Arith) §6.4
         expr    (consume_copy(v0) + consume_copy(v0))
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value 10
  [10]  d1    (D-Seq) §6.7
         expr    { @drop(v0); (consume_copy(v0) + consume_copy(v0)) }
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = RCopy { 5 }]
         result  value 10
  [11]  d0  (D-EndScope) §6.7 (retire the binding)
         expr    { let v0: RCopy = RCopy { value: 5 }; { @drop(v0); (consume_copy(v0) + consume_…
         store   [ℓ0 = RCopy { 5 }]
           →     [ℓ0 = † (retired)]
         result  value 10

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 10
         drop trace: (no drops)

