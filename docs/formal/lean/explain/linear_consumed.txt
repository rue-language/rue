════════════════════════════════════════════════════════════════════════════════
 linear_consumed
════════════════════════════════════════════════════════════════════════════════
A linear resource moved into a new binding and discharged there: the move
transfers the obligation and @drop is what finally satisfies it; S2 declares
no destructor, so nothing prints.

Rules exercised: (Use-Move) §5.1 · (@Drop) §5.3 · 3.9:39

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S2 = S2 { x0: 7 };
        {
            let v1: S2 = v0;
            {
                @drop(v1);
                7
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S2 = S2 { x0: 7 }; { let v1: S2 = v0; { @drop(v1); 7 } } }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Struct-Intro) §5.8
        e       S2 { x0: 7 }
        Γ;Σ     (empty)
        ⇒       S2  ⊣  (empty)
          (Lit) §5.8
            e       7
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: S2 = v0; { @drop(v1); 7 } }
        Γ;Σ     [v0: S2 = Owned]
        ⇒       i64  ⊣  [v0: S2 = MovedOut]
          (Use-Move) §5.1
            e       v0
            Γ;Σ     [v0: S2 = Owned]
            ⇒       S2  ⊣  [v0: S2 = MovedOut]
          (Seq) §5.3, 3.8:64
            e       { @drop(v1); 7 }
            Γ;Σ     [v1: S2 = Owned, v0: S2 = MovedOut]
            ⇒       i64  ⊣  [v1: S2 = MovedOut, v0: S2 = MovedOut]
              (@Drop) §5.3
                e       @drop(v1)
                Γ;Σ     [v1: S2 = Owned, v0: S2 = MovedOut]
                ⇒       ()  ⊣  [v1: S2 = MovedOut, v0: S2 = MovedOut]
              (Lit) §5.8
                e       7
                Γ;Σ     [v1: S2 = MovedOut, v0: S2 = MovedOut]
                ⇒       i64  ⊣  [v1: S2 = MovedOut, v0: S2 = MovedOut]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    7
         store   (empty)
           →     (empty)
         result  value 7
  [3]   d3        (D-Struct) §6.5 (mint #0)
         expr    S2 { x0: 7 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S2 { 7 }#0
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S2 { 7 }#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = S2 { 7 }#0]
         result  value S2 { 7 }#0
  [5]   d4          (D-Use-Move) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = S2 { 7 }#0]
           →     [ℓ0 = †, ℓ1 = ⊘]
         result  value S2 { 7 }#0
  [6]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = S2 { 7 }#0 at ℓ2
         store   [ℓ0 = †, ℓ1 = ⊘]
           →     [ℓ0 = †, ℓ1 = ⊘, ℓ2 = S2 { 7 }#0]
         result  value S2 { 7 }#0
  [7]   d5            @drop §6.11
         expr    @drop(v1)
         store   [ℓ0 = †, ℓ1 = ⊘, ℓ2 = S2 { 7 }#0]
           →     [ℓ0 = †, ℓ1 = ⊘, ℓ2 = ⊘]
         events  >> drop ℓ2 = S2 { 7 }#0
         result  value ()
  [8]   d5            literal §6.3
         expr    7
         store   [ℓ0 = †, ℓ1 = ⊘, ℓ2 = ⊘]
           →     [ℓ0 = †, ℓ1 = ⊘, ℓ2 = ⊘]
         result  value 7
  [9]   d4          (D-Seq) §6.7
         expr    { @drop(v1); 7 }
         store   [ℓ0 = †, ℓ1 = ⊘, ℓ2 = S2 { 7 }#0]
           →     [ℓ0 = †, ℓ1 = ⊘, ℓ2 = ⊘]
         result  value 7
  [10]  d3        (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ2])
         store   [ℓ0 = †, ℓ1 = ⊘, ℓ2 = ⊘]
           →     [ℓ0 = †, ℓ1 = ⊘, ℓ2 = †]
         result  value 7
  [11]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ1])
         store   [ℓ0 = †, ℓ1 = ⊘, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value 7
  [12]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †]
         result  value 7

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 7
         drop trace: drop ℓ2 = S2 { 7 }#0

