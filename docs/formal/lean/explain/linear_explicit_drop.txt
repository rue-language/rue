════════════════════════════════════════════════════════════════════════════════
 linear_explicit_drop
════════════════════════════════════════════════════════════════════════════════
A linear resource discharged by the core's @drop (the only non-move discharge
of a linear obligation); printed as a consuming read, since RLinear cannot
carry a destructor (Print.lean).

Rules exercised: (@Drop) §5.3 · 3.9:39

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        let v0: RLinear = RLinear { value: 9 };
        {
            @dbg(consume_linear(v0));
            3
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: RLinear = RLinear { value: 9 }; { @dbg(consume_linear(v0)); 3 } }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
        e       RLinear { value: 9 }
        Γ;Σ     (empty)
        ⇒       RLinear  ⊣  (empty)
          (Lit) §5.8
            e       9
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @dbg(consume_linear(v0)); 3 }
        Γ;Σ     [v0: RLinear = Owned]
        ⇒       i64  ⊣  [v0: RLinear = MovedOut]
          (@Drop) §5.3 (the printed program spells this as a consuming read; see the program)
            e       @dbg(consume_linear(v0))
            Γ;Σ     [v0: RLinear = Owned]
            ⇒       ()  ⊣  [v0: RLinear = MovedOut]
          (Lit) §5.8
            e       3
            Γ;Σ     [v0: RLinear = MovedOut]
            ⇒       i64  ⊣  [v0: RLinear = MovedOut]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    9
         store   (empty)
           →     (empty)
         result  value 9
  [3]   d3        resource intro §6.5
         expr    RLinear { value: 9 }
         store   (empty)
           →     (empty)
         result  value RLinear { 9 }
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = RLinear { 9 } at ℓ0
         store   (empty)
           →     [ℓ0 = RLinear { 9 }]
         result  value RLinear { 9 }
  [5]   d4          @drop §6.11 (the printed program spells this as a consuming read; see the program)
         expr    @dbg(consume_linear(v0))
         store   [ℓ0 = RLinear { 9 }]
           →     [ℓ0 = ⊘ (moved out)]
         events  >> drop ℓ0 = RLinear { 9 }
         result  value ()
  [6]   d4          literal §6.3
         expr    3
         store   [ℓ0 = ⊘ (moved out)]
           →     [ℓ0 = ⊘ (moved out)]
         result  value 3
  [7]   d3        (D-Seq) §6.7
         expr    { @dbg(consume_linear(v0)); 3 }
         store   [ℓ0 = RLinear { 9 }]
           →     [ℓ0 = ⊘ (moved out)]
         result  value 3
  [8]   d2      (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = ⊘ (moved out)]
           →     [ℓ0 = † (retired)]
         result  value 3
  [9]   d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   [ℓ0 = † (retired)]
           →     [ℓ0 = † (retired)]
         result  value 3

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 3
         drop trace: drop ℓ0 = RLinear { 9 }

