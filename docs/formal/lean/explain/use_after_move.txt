════════════════════════════════════════════════════════════════════════════════
 use_after_move
════════════════════════════════════════════════════════════════════════════════
A moved affine binding used again: rejected statically (E0205) and refused
dynamically (useAfterMove).

Rules exercised: (Use-Move) §5.1 · 3.8:5

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        let v0: RAffine = RAffine { value: 1, live: true };
        {
            let v1: RAffine = v0;
            consume_affine(v0)
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Use-Copy)/(Use-Move) §5.1:

    on  v0

    a use of a place whose Σ state is MovedOut — the value was already moved
    out ((Use-Copy)/(Use-Move) premise `Σ(p) = Owned`, §5.1; 3.8:5; the
    compiler reports E0205)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: RAffine = RAffine { value: 1, live: true }; { let v1: RAffine = v0; c…
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
        e       RAffine { value: 1, live: true }
        Γ;Σ     (empty)
        ⇒       RAffine  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: RAffine = v0; consume_affine(v0) }
        Γ;Σ     [v0: RAffine = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (Use-Move) §5.1
            e       v0
            Γ;Σ     [v0: RAffine = Owned]
            ⇒       RAffine  ⊣  [v0: RAffine = MovedOut]
          resource elimination §5.8/§4.2 (an abstract consuming elimination)
            e       consume_affine(v0)
            Γ;Σ     [v1: RAffine = Owned, v0: RAffine = MovedOut]
            ✗       this premise fails:
                    a premise's own derivation failed — the reason is the rejected
                    premise nested under this rule
              (Use-Copy)/(Use-Move) §5.1
                e       v0
                Γ;Σ     [v1: RAffine = Owned, v0: RAffine = MovedOut]
                ✗       this premise fails:
                        a use of a place whose Σ state is MovedOut — the value was already
                        moved out ((Use-Copy)/(Use-Move) premise `Σ(p) = Owned`, §5.1;
                        3.8:5; the compiler reports E0205)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d3        resource intro §6.5
         expr    RAffine { value: 1, live: true }
         store   (empty)
           →     (empty)
         result  value RAffine { 1 }
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = RAffine { 1 } at ℓ0
         store   (empty)
           →     [ℓ0 = RAffine { 1 }]
         result  value RAffine { 1 }
  [5]   d4          (D-Use-Move) §6.3
         expr    v0
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = ⊘ (moved out)]
         result  value RAffine { 1 }
  [6]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = RAffine { 1 } at ℓ1
         store   [ℓ0 = ⊘ (moved out)]
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  value RAffine { 1 }
  [7]   d5            (D-Use-Copy)/(D-Use-Move) §6.3
         expr    v0
         store   [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  REFUSED (useAfterMove)
                 a read of a cell holding ⊘: the value was already moved out of this
                 place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)
  [8]   d4          resource elimination §6.5 — a premise did not complete
         expr    consume_affine(v0)
         store   [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  REFUSED (useAfterMove)
                 a read of a cell holding ⊘: the value was already moved out of this
                 place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)
  [9]   d3        (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return runs the drop through the frame's scope record instead)
         expr    { let v1: RAffine = v0; consume_affine(v0) }
         store   [ℓ0 = RAffine { 1 }]
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  REFUSED (useAfterMove)
                 a read of a cell holding ⊘: the value was already moved out of this
                 place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)
  [10]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return runs the drop through the frame's scope record instead)
         expr    { let v0: RAffine = RAffine { value: 1, live: true }; { let v1: RAffine = v0; c…
         store   (empty)
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  REFUSED (useAfterMove)
                 a read of a cell holding ⊘: the value was already moved out of this
                 place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)
  [11]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = ⊘ (moved out), ℓ1 = RAffine { 1 }]
         result  REFUSED (useAfterMove)
                 a read of a cell holding ⊘: the value was already moved out of this
                 place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (useAfterMove)
         a read of a cell holding ⊘: the value was already moved out of this
         place ((D-Use-Move) §6.3; 3.8:5; §7 “no use after move”)

