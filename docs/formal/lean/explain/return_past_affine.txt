════════════════════════════════════════════════════════════════════════════════
 return_past_affine
════════════════════════════════════════════════════════════════════════════════
An early return past two live affine bindings: the frame unwinds newest-first,
so the drops print 4 then 3, then the value 7.

Rules exercised: (Return-Value) §5.7 · (D-Return) §6.9 · 3.9:18 · 3.9:4

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        let v0: RAffine = RAffine { value: 3, live: true };
        {
            let v1: RAffine = RAffine { value: 4, live: true };
            return 7
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: RAffine = RAffine { value: 3, live: true }; { let v1: RAffine = RAffi…
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
        e       RAffine { value: 3, live: true }
        Γ;Σ     (empty)
        ⇒       RAffine  ⊣  (empty)
          (Lit) §5.8
            e       3
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: RAffine = RAffine { value: 4, live: true }; return 7 }
        Γ;Σ     [v0: RAffine = Owned]
        ⇒       i64  ⊣  [v0: RAffine = Owned]
          resource intro §5.8 (the shape of the aggregate-introduction rule; `res κ` has no fields)
            e       RAffine { value: 4, live: true }
            Γ;Σ     [v0: RAffine = Owned]
            ⇒       RAffine  ⊣  [v0: RAffine = Owned]
              (Lit) §5.8
                e       4
                Γ;Σ     [v0: RAffine = Owned]
                ⇒       i64  ⊣  [v0: RAffine = Owned]
          (Return-Value) §5.7
            e       return 7
            Γ;Σ     [v1: RAffine = Owned, v0: RAffine = Owned]
            ⇒       i64  ⊣  [v1: RAffine = Owned, v0: RAffine = Owned]
              (Lit) §5.8
                e       7
                Γ;Σ     [v1: RAffine = Owned, v0: RAffine = Owned]
                ⇒       i64  ⊣  [v1: RAffine = Owned, v0: RAffine = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    3
         store   (empty)
           →     (empty)
         result  value 3
  [3]   d3        resource intro §6.5
         expr    RAffine { value: 3, live: true }
         store   (empty)
           →     (empty)
         result  value RAffine { 3 }
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = RAffine { 3 } at ℓ0
         store   (empty)
           →     [ℓ0 = RAffine { 3 }]
         result  value RAffine { 3 }
  [5]   d5            literal §6.3
         expr    4
         store   [ℓ0 = RAffine { 3 }]
           →     [ℓ0 = RAffine { 3 }]
         result  value 4
  [6]   d4          resource intro §6.5
         expr    RAffine { value: 4, live: true }
         store   [ℓ0 = RAffine { 3 }]
           →     [ℓ0 = RAffine { 3 }]
         result  value RAffine { 4 }
  [7]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = RAffine { 4 } at ℓ1
         store   [ℓ0 = RAffine { 3 }]
           →     [ℓ0 = RAffine { 3 }, ℓ1 = RAffine { 4 }]
         result  value RAffine { 4 }
  [8]   d5            literal §6.3
         expr    7
         store   [ℓ0 = RAffine { 3 }, ℓ1 = RAffine { 4 }]
           →     [ℓ0 = RAffine { 3 }, ℓ1 = RAffine { 4 }]
         result  value 7
  [9]   d4          (D-Return) §6.9 (unwind the frame)
         expr    run-all-scope-drops([ℓ1, ℓ0])
         store   [ℓ0 = RAffine { 3 }, ℓ1 = RAffine { 4 }]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         events  >> drop ℓ1 = RAffine { 4 }; drop ℓ0 = RAffine { 3 }
         result  value 7 — handed past this node by a `return` (§6.9)
  [10]  d3        (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return runs the drop through the frame's scope record instead)
         expr    { let v1: RAffine = RAffine { value: 4, live: true }; return 7 }
         store   [ℓ0 = RAffine { 3 }]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 7 — handed past this node by a `return` (§6.9)
  [11]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return runs the drop through the frame's scope record instead)
         expr    { let v0: RAffine = RAffine { value: 3, live: true }; { let v1: RAffine = RAffi…
         store   (empty)
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 7 — handed past this node by a `return` (§6.9)
  [12]  d0  (D-Return)/(D-Return-Main) §6.9 (the callee's return is the call's value)
         expr    absorb 7
         store   [ℓ0 = † (retired), ℓ1 = † (retired)]
           →     [ℓ0 = † (retired), ℓ1 = † (retired)]
         result  value 7

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 7
         drop trace: drop ℓ1 = RAffine { 4 }; drop ℓ0 = RAffine { 3 }

