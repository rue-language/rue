════════════════════════════════════════════════════════════════════════════════
 return_past_affine
════════════════════════════════════════════════════════════════════════════════
An early return past two live affine bindings: the frame unwinds newest-first,
so the destructors print 4 then 3, then the value 7.

Rules exercised: (Return-Value) §5.7 · (D-Return) §6.9 · 3.9:18 · 3.9:4

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S1 = S1 { x0: 3 };
        {
            let v1: S1 = S1 { x0: 4 };
            return 7
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ never, exit ⊥ (no normal exit)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3, the body diverging
    e       { let v0: S1 = S1 { x0: 3 }; { let v1: S1 = S1 { x0: 4 }; return 7 } }
    Γ;Σ     (empty)
    ⇒       never  ⊣  ⊥
      (Struct-Intro) §5.8
        e       S1 { x0: 3 }
        Γ;Σ     (empty)
        ⇒       S1  ⊣  (empty)
          (Lit) §5.8
            e       3
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Let) §5.3, the body diverging
        e       { let v1: S1 = S1 { x0: 4 }; return 7 }
        Γ;Σ     [v0: S1 = Owned]
        ⇒       never  ⊣  ⊥
          (Struct-Intro) §5.8
            e       S1 { x0: 4 }
            Γ;Σ     [v0: S1 = Owned]
            ⇒       S1  ⊣  [v0: S1 = Owned]
              (Lit) §5.8
                e       4
                Γ;Σ     [v0: S1 = Owned]
                ⇒       i64  ⊣  [v0: S1 = Owned]
          (Return-Value) §5.7
            e       return 7
            Γ;Σ     [v1: S1 = Owned, v0: S1 = Owned]
            ⇒       never  ⊣  ⊥
              (Lit) §5.8
                e       7
                Γ;Σ     [v1: S1 = Owned, v0: S1 = Owned]
                ⇒       i64  ⊣  [v1: S1 = Owned, v0: S1 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    3
         store   (empty)
           →     (empty)
         result  value 3
  [3]   d3        (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 3 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 3 }#0
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S1 { 3 }#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = S1 { 3 }#0]
         result  value S1 { 3 }#0
  [5]   d5            literal §6.3
         expr    4
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0]
           →     [ℓ0 = †, ℓ1 = S1 { 3 }#0]
         result  value 4
  [6]   d4          (D-Struct) §6.5 (mint #2)
         expr    S1 { x0: 4 }
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0]
           →     [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †]
         result  value S1 { 4 }#2
  [7]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = S1 { 4 }#2 at ℓ3
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †]
           →     [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †, ℓ3 = S1 { 4 }#2]
         result  value S1 { 4 }#2
  [8]   d5            literal §6.3
         expr    7
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †, ℓ3 = S1 { 4 }#2]
           →     [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †, ℓ3 = S1 { 4 }#2]
         result  value 7
  [9]   d4          (D-Return) §6.9 (unwind the frame)
         expr    run-all-scope-drops([ℓ3, ℓ1])
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0, ℓ2 = †, ℓ3 = S1 { 4 }#2]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         events  >> drop ℓ3 = S1 { 4 }#2; run drop fn S1(S1 { 4 }#2); drop ℓ1 = S1 { 3 }#0; run drop fn S1(S1 { 3 }#0)
         result  value 7 — handed past this node by a `return` (§6.9)
  [10]  d3        (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v1: S1 = S1 { x0: 4 }; return 7 }
         store   [ℓ0 = †, ℓ1 = S1 { 3 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 7 — handed past this node by a `return` (§6.9)
  [11]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v0: S1 = S1 { x0: 3 }; { let v1: S1 = S1 { x0: 4 }; return 7 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 7 — handed past this node by a `return` (§6.9)
  [12]  d0  (D-Return)/(D-Return-Main) §6.9 (the callee's return is the call's value)
         expr    absorb 7
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = †, ℓ3 = †]
         result  value 7

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 7
         drop trace: drop ℓ3 = S1 { 4 }#2; run drop fn S1(S1 { 4 }#2); drop ℓ1 = S1 { 3 }#0; run drop fn S1(S1 { 3 }#0)

