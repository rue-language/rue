════════════════════════════════════════════════════════════════════════════════
 enum_arm_leaks_payload
════════════════════════════════════════════════════════════════════════════════
An arm binds a linear payload and leaves it: §5.6's check at the arm's end is
the leak (E0406 on the binding), and the machine refuses with linearLeak.

Rules exercised: (Match) §5.5 · §5.6 residual-linear leak check · 6.3:17 · 3.8:32

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
struct S11 { x0: E0, x1: S1 }
enum E0 { K0(S1), K1 }
enum E1 { K0(S3), K1 }
enum E2 { K0(S1, S1), K1 }
enum E3 { K0, K1 }
enum E4 { K0(i64), K1 }
enum E5 { K0(S1, S3), K1 }
fn f0() -> i64 {
    {
        let v0: E1 = E1.K0(S3 { x0: 1 });
        match v0 {
            E1.K0(v1) => {
                5
            },
            E1.K1 => {
                6
            },
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (Match) §5.5:

    on  match v0 { E1.K0(v1) => 5, E1.K1 => 6 }

    an arm leaves one of its payload locals Owned at a linear type where the
    arm ends its scope, which is §5.6's leak check on the payload binding
    ((Match) premise `each pattern local x_{ij} leaves scope at arm end`,
    §5.5; 6.3:17, 3.8:32; the compiler reports E0406)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: E1 = E1.K0(S3 { x0: 1 }); match v0 { E1.K0(v1) => 5, E1.K1 => 6 } }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Enum-Intro) §5.5
        e       E1.K0(S3 { x0: 1 })
        Γ;Σ     (empty)
        ⇒       E1  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S3 { x0: 1 }
            Γ;Σ     (empty)
            ⇒       S3  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Match) §5.5
        e       match v0 { E1.K0(v1) => 5, E1.K1 => 6 }
        Γ;Σ     [v0: E1 = Owned]
        ✗       this premise fails:
                an arm leaves one of its payload locals Owned at a linear type where
                the arm ends its scope, which is §5.6's leak check on the payload
                binding ((Match) premise `each pattern local x_{ij} leaves scope at
                arm end`, §5.5; 6.3:17, 3.8:32; the compiler reports E0406)
          (Use-Move) §5.1
            e       v0
            Γ;Σ     [v0: E1 = Owned]
            ⇒       E1  ⊣  [v0: E1 = MovedOut]
          (Lit) §5.8
            e       5
            Γ;Σ     [v1: S3 = Owned, v0: E1 = MovedOut]
            ⇒       i64  ⊣  [v1: S3 = Owned, v0: E1 = MovedOut]
          (Lit) §5.8
            e       6
            Γ;Σ     [v0: E1 = MovedOut]
            ⇒       i64  ⊣  [v0: E1 = MovedOut]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d4          (D-Struct) §6.5 (mint #0)
         expr    S3 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S3 { 1 }#0
  [4]   d3        (D-Enum-Intro) §6.6 (mint #1)
         expr    E1.K0(S3 { x0: 1 })
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value E1.K0⟨S3 { 1 }#0⟩#1
  [5]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = E1.K0⟨S3 { 1 }#0⟩#1 at ℓ2
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = E1.K0⟨S3 { 1 }#0⟩#1]
         result  value E1.K0⟨S3 { 1 }#0⟩#1
  [6]   d4          (D-Use-Move) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = E1.K0⟨S3 { 1 }#0⟩#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘]
         result  value E1.K0⟨S3 { 1 }#0⟩#1
  [7]   d4          (D-Match) §6.6 (bind the arm's payload)
         expr    bind E1.K0's payload to [ℓ3]
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
         events  >> consume E1.K0⟨⊘⟩#1
         result  value E1.K0⟨S3 { 1 }#0⟩#1
  [8]   d4          literal §6.3
         expr    5
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
         result  value 5
  [9]   d3        (D-EndScope) §6.6 (end the arm)
         expr    match v0 { E1.K0(v1) => 5, E1.K1 => 6 }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [10]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v0: E1 = E1.K0(S3 { x0: 1 }); match v0 { E1.K0(v1) => 5, E1.K1 => 6 } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [11]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = ⊘, ℓ3 = S3 { 1 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity, and the step numbers there are the order `drop_order` fixes;
`[n.k]` is the k-th end of step n, when one step ends several values.

  id    minted  ended                             destructor  ended
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     —                                 —           the run was refused (§6)
  #1    [4]     [7] consume                       —           once

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

