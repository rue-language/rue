════════════════════════════════════════════════════════════════════════════════
 index_drop_copy_checker
════════════════════════════════════════════════════════════════════════════════
@drop of a non-Copy element through a dynamic index — the index a call to a
second function that returns its argument unchanged, so the compiler's
constant folder (8.2:4) cannot reduce it to a compile-time constant: the
checker has no test at Untrackable(OrdinaryDynamic) beside
(Use-Untrackable-Dynamic-Copy) §5.1's Copy-element one (the compiler: E0904);
the typing rule for @drop is unaffected, only the checker's transcription of
it. The machine refuses the same drop with typeConfusion. Seeded by the
definition mutation analysis (RUE-2486): mutant `index-drop-copy-checker`.

Rules exercised: (Use-Untrackable-Dynamic-Copy) §5.1 · (@Drop) §5.3

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: [S1; 1] = [S1 { x0: 1 }];
        {
            @drop(v0[{ let t3i0: i64 = f1(0); t3i0 }]);
            {
                { let t4g: i64 = 1; @dbg(t4g) };
                0
            }
        }
    }
}

fn f1(v0: i64) -> i64 {
    v0
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (@Drop-Copy) §5.3 below a dynamic index, with (Use-Untrackable-Dynamic-Copy) §5.1's premises:

    on  @drop(v0[f1(0)])

    the type S1 read below the dynamic index is not `Copy`, and a dynamic
    index has no rule there: the compiler cannot know which element a runtime
    index moved ((Use-Untrackable-Dynamic-Copy) §5.1; 3.8:70, 7.1:28; the
    compiler reports E0904)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: [S1; 1] = [S1 { x0: 1 }]; { @drop(v0[f1(0)]); { @dbg(1); 0 } } }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Array-Intro) §5.8
        e       [S1 { x0: 1 }]
        Γ;Σ     (empty)
        ⇒       [S1; 1]  ⊣  (empty)
          (Struct-Intro) §5.8
            e       S1 { x0: 1 }
            Γ;Σ     (empty)
            ⇒       S1  ⊣  (empty)
              (Lit) §5.8
                e       1
                Γ;Σ     (empty)
                ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @drop(v0[f1(0)]); { @dbg(1); 0 } }
        Γ;Σ     [v0: [S1; 1] = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (@Drop-Copy) §5.3 below a dynamic index, with (Use-Untrackable-Dynamic-Copy) §5.1's premises
            e       @drop(v0[f1(0)])
            Γ;Σ     [v0: [S1; 1] = Owned]
            ✗       this premise fails:
                    the type S1 read below the dynamic index is not `Copy`, and a
                    dynamic index has no rule there: the compiler cannot know which
                    element a runtime index moved ((Use-Untrackable-Dynamic-Copy) §5.1;
                    3.8:70, 7.1:28; the compiler reports E0904)
              (Call) §5.8
                e       f1(0)
                Γ;Σ     [v0: [S1; 1] = Owned]
                ⇒       i64  ⊣  [v0: [S1; 1] = Owned]
                  (Lit) §5.8
                    e       0
                    Γ;Σ     [v0: [S1; 1] = Owned]
                    ⇒       i64  ⊣  [v0: [S1; 1] = Owned]

  fn f1(v0: i64) -> i64

  (Use-Copy) §5.1
    e       v0
    Γ;Σ     [v0: i64 = Owned]
    ⇒       i64  ⊣  [v0: i64 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d4          (D-Struct) §6.5 (mint #0)
         expr    S1 { x0: 1 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S1 { 1 }#0
  [4]   d3        (D-Array) §6.5 (mint #1)
         expr    [S1 { x0: 1 }]
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †]
         result  value [S1 { 1 }#0]#1
  [5]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = [S1 { 1 }#0]#1 at ℓ2
         store   [ℓ0 = †, ℓ1 = †]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
         result  value [S1 { 1 }#0]#1
  [6]   d7                literal §6.3
         expr    0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
         result  value 0
  [7]   d7                (D-Call) §6.9 (push the frame)
         expr    fn f1(v0: i64) -> i64  with [ℓ3]
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = 0]
         result  value ()
  [8]   d8                  (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = 0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = 0]
         result  value 0
  [9]   d6              (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([ℓ3])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = 0]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  value 0
  [10]  d5            (D-Index) §6.5
         expr    v0[f1(0)]
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  REFUSED (typeConfusion)
                 an operator met a wrong-shaped value, a call's argument count did
                 not match its callee's parameter list, or a dynamic-index read, a
                 dynamic-index @drop or a repeat met a non-Copy value
                 ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
                 exclude all three, and `soundness` (§7) is the proof
  [11]  d4          @drop §6.11 at a Copy place below a dynamic index — a premise did not complete
         expr    @drop(v0[f1(0)])
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  REFUSED (typeConfusion)
                 an operator met a wrong-shaped value, a call's argument count did
                 not match its callee's parameter list, or a dynamic-index read, a
                 dynamic-index @drop or a repeat met a non-Copy value
                 ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
                 exclude all three, and `soundness` (§7) is the proof
  [12]  d3        (D-Seq) §6.7 — a premise did not complete
         expr    { @drop(v0[f1(0)]); { @dbg(1); 0 } }
         store   [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1]
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  REFUSED (typeConfusion)
                 an operator met a wrong-shaped value, a call's argument count did
                 not match its callee's parameter list, or a dynamic-index read, a
                 dynamic-index @drop or a repeat met a non-Copy value
                 ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
                 exclude all three, and `soundness` (§7) is the proof
  [13]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v0: [S1; 1] = [S1 { x0: 1 }]; { @drop(v0[f1(0)]); { @dbg(1); 0 } } }
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  REFUSED (typeConfusion)
                 an operator met a wrong-shaped value, a call's argument count did
                 not match its callee's parameter list, or a dynamic-index read, a
                 dynamic-index @drop or a repeat met a non-Copy value
                 ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
                 exclude all three, and `soundness` (§7) is the proof
  [14]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = †, ℓ2 = [S1 { 1 }#0]#1, ℓ3 = †]
         result  REFUSED (typeConfusion)
                 an operator met a wrong-shaped value, a call's argument count did
                 not match its callee's parameter list, or a dynamic-index read, a
                 dynamic-index @drop or a repeat met a non-Copy value
                 ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
                 exclude all three, and `soundness` (§7) is the proof

── Identities (§7) ─────────────────────────────────────────────────────────────

One line per owned identity: the step that minted it, the steps that ended
it (a drop marker, a discarded temporary, a consumption), and the steps whose
destructor ran on it. `drop_exactly_once` is one entry in the ended column
per identity. Read in row order, and `[n.k]` (the k-th end of row n) inside a
row, the ends are the trace's order, which `drop_order` fixes: a value's
destructors inside its drop in §6.11's order, and cells last-in first-out.

  id    minted  ended                             destructor  verdict
  ────────────────────────────────────────────────────────────────────────
  #0    [3]     —                                 —           the run was refused (§6)
  #1    [4]     —                                 —           the run was refused (§6)

  The run was refused (§6), which a checked program's never is (`soundness`);
  the ledger is what the table recorded before the refusal.

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (typeConfusion)
         an operator met a wrong-shaped value, a call's argument count did
         not match its callee's parameter list, or a dynamic-index read, a
         dynamic-index @drop or a repeat met a non-Copy value
         ((D-Use-Untrackable-Dynamic-Copy) §6.3, 7.1:38); the statics (§5)
         exclude all three, and `soundness` (§7) is the proof

