════════════════════════════════════════════════════════════════════════════════
 float_to_int_trunc
════════════════════════════════════════════════════════════════════════════════
@float_to_int truncates toward zero, on both signs.

Rules exercised: (Float-To-Int) §5.8 · (D-Float-To-Int) §6.4 · 3.12:17

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i64 {
    {
        { let t2g: i32 = { let t3c: f64 = 2.9; let t3k: i32 = @float_to_int(t3c); t3k }; @dbg(t2g) };
        {
            { let t3g: i32 = { let t4c: f64 = (-2.9); let t4k: i32 = @float_to_int(t4c); t4k }; @dbg(t3g) };
            0
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Seq) §5.3, 3.8:64
    e       { @dbg(@float_to_int(2.9)); { @dbg(@float_to_int((-2.9))); 0 } }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Dbg) §5.8
        e       @dbg(@float_to_int(2.9))
        Γ;Σ     (empty)
        ⇒       ()  ⊣  (empty)
          (Float-To-Int) §5.8
            e       @float_to_int(2.9)
            Γ;Σ     (empty)
            ⇒       i32  ⊣  (empty)
              (Lit) §5.8
                e       2.9
                Γ;Σ     (empty)
                ⇒       f64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { @dbg(@float_to_int((-2.9))); 0 }
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
          (Dbg) §5.8
            e       @dbg(@float_to_int((-2.9)))
            Γ;Σ     (empty)
            ⇒       ()  ⊣  (empty)
              (Float-To-Int) §5.8
                e       @float_to_int((-2.9))
                Γ;Σ     (empty)
                ⇒       i32  ⊣  (empty)
                  (Float-Neg) §5.8
                    e       (-2.9)
                    Γ;Σ     (empty)
                    ⇒       f64  ⊣  (empty)
                      (Lit) §5.8
                        e       2.9
                        Γ;Σ     (empty)
                        ⇒       f64  ⊣  (empty)
          (Lit) §5.8
            e       0
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d5            literal §6.3 (3.12:9 rounds the decimal)
         expr    2.9
         store   (empty)
           →     (empty)
         result  value 2.9
  [3]   d4          (D-Float-To-Int) §6.4
         expr    @float_to_int(2.9)
         store   (empty)
           →     (empty)
         result  value 2
  [4]   d3        (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(@float_to_int(2.9))
         store   (empty)
           →     (empty)
         events  >> @dbg prints 2
         result  value ()
  [5]   d7                literal §6.3 (3.12:9 rounds the decimal)
         expr    2.9
         store   (empty)
           →     (empty)
         result  value 2.9
  [6]   d6              (D-Float-Neg) §6.4
         expr    (-2.9)
         store   (empty)
           →     (empty)
         result  value -2.9
  [7]   d5            (D-Float-To-Int) §6.4
         expr    @float_to_int((-2.9))
         store   (empty)
           →     (empty)
         result  value -2
  [8]   d4          (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(@float_to_int((-2.9)))
         store   (empty)
           →     (empty)
         events  >> @dbg prints -2
         result  value ()
  [9]   d4          literal §6.3
         expr    0
         store   (empty)
           →     (empty)
         result  value 0
  [10]  d3        (D-Seq) §6.7
         expr    { @dbg(@float_to_int((-2.9))); 0 }
         store   (empty)
           →     (empty)
         result  value 0
  [11]  d2      (D-Seq) §6.7
         expr    { @dbg(@float_to_int(2.9)); { @dbg(@float_to_int((-2.9))); 0 } }
         store   (empty)
           →     (empty)
         result  value 0
  [12]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    run-all-scope-drops([])
         store   (empty)
           →     (empty)
         result  value 0

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 0
         drop trace: @dbg prints 2; @dbg prints -2

