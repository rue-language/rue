════════════════════════════════════════════════════════════════════════════════
 array_zero_length_field_dyn_read
════════════════════════════════════════════════════════════════════════════════
h.x1[i] at i = 0, where the field x1 is an [i64; 0]: every index into a
zero-length array is out of bounds, so the read takes (D-Index-Trap) §6.5's
bounds trap and nothing prints. The bridge was red on this one until RUE-2345
was fixed: the compiler reported an internal error in code generation (a
zero-sized place reached through a field was not diverted to the zero-sized
address). It now traps as the model does, and the case stays as the regression
signal.

Rules exercised: (Use-Untrackable-Dynamic-Copy) §5.1 · (D-Index-Trap) §6.5 · 7.1:11

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
struct S11 { x0: i8, x1: [i64; 0] }
fn f0() -> i64 {
    {
        let v0: S11 = S11 { x0: 8, x1: [] };
        {
            let v1: i32 = 0;
            {
                { let t4g: i64 = v0.x1[{ let t5i0: i32 = v1; t5i0 }]; @dbg(t4g) };
                0
            }
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i64  — body ⇒ i64, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S11 = S11 { x0: 8, x1: [] }; { let v1: i32 = 0; { @dbg(v0.x1[v1]); 0 …
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Struct-Intro) §5.8
        e       S11 { x0: 8, x1: [] }
        Γ;Σ     (empty)
        ⇒       S11  ⊣  (empty)
          (Lit) §5.8
            e       8
            Γ;Σ     (empty)
            ⇒       i8  ⊣  (empty)
          (Array-Intro) §5.8
            e       []
            Γ;Σ     (empty)
            ⇒       [i64; 0]  ⊣  (empty)
      (Let) §5.3 + the §5.6 scope-exit leak check
        e       { let v1: i32 = 0; { @dbg(v0.x1[v1]); 0 } }
        Γ;Σ     [v0: S11 = Owned]
        ⇒       i64  ⊣  [v0: S11 = Owned]
          (Lit) §5.8
            e       0
            Γ;Σ     [v0: S11 = Owned]
            ⇒       i32  ⊣  [v0: S11 = Owned]
          (Seq) §5.3, 3.8:64
            e       { @dbg(v0.x1[v1]); 0 }
            Γ;Σ     [v1: i32 = Owned, v0: S11 = Owned]
            ⇒       i64  ⊣  [v1: i32 = Owned, v0: S11 = Owned]
              (Dbg) §5.8
                e       @dbg(v0.x1[v1])
                Γ;Σ     [v1: i32 = Owned, v0: S11 = Owned]
                ⇒       ()  ⊣  [v1: i32 = Owned, v0: S11 = Owned]
                  (Use-Untrackable-Dynamic-Copy) §5.1
                    e       v0.x1[v1]
                    Γ;Σ     [v1: i32 = Owned, v0: S11 = Owned]
                    ⇒       i64  ⊣  [v1: i32 = Owned, v0: S11 = Owned]
                      (Use-Copy) §5.1
                        e       v1
                        Γ;Σ     [v1: i32 = Owned, v0: S11 = Owned]
                        ⇒       i32  ⊣  [v1: i32 = Owned, v0: S11 = Owned]
              (Lit) §5.8
                e       0
                Γ;Σ     [v1: i32 = Owned, v0: S11 = Owned]
                ⇒       i64  ⊣  [v1: i32 = Owned, v0: S11 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    8
         store   (empty)
           →     (empty)
         result  value 8
  [3]   d4          (D-Array) §6.5
         expr    []
         store   (empty)
           →     (empty)
         result  value []
  [4]   d3        (D-Struct) §6.5
         expr    S11 { x0: 8, x1: [] }
         store   (empty)
           →     (empty)
         result  value S11 { 8, [] }
  [5]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S11 { 8, [] } at ℓ0
         store   (empty)
           →     [ℓ0 = S11 { 8, [] }]
         result  value S11 { 8, [] }
  [6]   d4          literal §6.3
         expr    0
         store   [ℓ0 = S11 { 8, [] }]
           →     [ℓ0 = S11 { 8, [] }]
         result  value 0
  [7]   d4          (D-Let) §6.7 (mint the binding)
         expr    let v1 = 0 at ℓ1
         store   [ℓ0 = S11 { 8, [] }]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  value 0
  [8]   d7                (D-Use-Copy) §6.3
         expr    v1
         store   [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  value 0
  [9]   d6              (D-Index-Trap) §6.5 — the bounds trap of §6.12
         expr    v0.x1[v1]
         store   [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12
  [10]  d5            (Dbg) §5.8, the observable output of §6.12 — a premise did not complete
         expr    @dbg(v0.x1[v1])
         store   [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12
  [11]  d4          (D-Seq) §6.7 — a premise did not complete
         expr    { @dbg(v0.x1[v1]); 0 }
         store   [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12
  [12]  d3        (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v1: i32 = 0; { @dbg(v0.x1[v1]); 0 } }
         store   [ℓ0 = S11 { 8, [] }]
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12
  [13]  d2      (D-EndScope) §6.7 — the body did not complete, so this scope never closed (an early return or a break runs the drop through the frame's scope record instead; a trap runs no drop at all)
         expr    { let v0: S11 = S11 { x0: 8, x1: [] }; { let v1: i32 = 0; { @dbg(v0.x1[v1]); 0 …
         store   (empty)
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12
  [14]  d0  (Panic-Lift) §6.2 — the callee trapped, so no frame is popped: §6.12 abandons the configuration and `run-all-scope-drops` never runs
         expr    f0()
         store   (empty)
           →     [ℓ0 = S11 { 8, [] }, ℓ1 = 0]
         result  PANIC (bounds) — a defined trap, §6.12

────────────────────────────────────────────────────────────────────────────────
Outcome: PANIC (bounds) — a defined trap, §6.12
         trace up to the trap: (nothing)

