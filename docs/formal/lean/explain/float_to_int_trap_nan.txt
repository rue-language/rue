════════════════════════════════════════════════════════════════════════════════
 float_to_int_trap_nan
════════════════════════════════════════════════════════════════════════════════
@float_to_int of a NaN traps with overflow, after the stdout the run had
already produced.

Rules exercised: (D-Float-To-Int-Trap) §6.4 · 3.12:18 · 8.1:7

── The program ─────────────────────────────────────────────────────────────────

fn f0() -> i32 {
    {
        { let t2g: i64 = 1; @dbg(t2g) };
        { let t2c: f64 = (0.0 / 0.0); let t2k: i32 = @float_to_int(t2c); t2k }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         Every function is well-formed by (Fn) §5.8 and the entry point takes
         no parameters, so `checkProgram_sound` ties the whole program to a
         real derivation and the §7 safety theorems apply to it.

         fn f0() -> i32  — body ⇒ i32, exit Σ (empty)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i32

  (Seq) §5.3, 3.8:64
    e       { @dbg(1); @float_to_int((0.0 / 0.0)) }
    Γ;Σ     (empty)
    ⇒       i32  ⊣  (empty)
      (Dbg) §5.8
        e       @dbg(1)
        Γ;Σ     (empty)
        ⇒       ()  ⊣  (empty)
          (Lit) §5.8
            e       1
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Float-To-Int) §5.8
        e       @float_to_int((0.0 / 0.0))
        Γ;Σ     (empty)
        ⇒       i32  ⊣  (empty)
          (Float-Arith) §5.8
            e       (0.0 / 0.0)
            Γ;Σ     (empty)
            ⇒       f64  ⊣  (empty)
              (Lit) §5.8
                e       0.0
                Γ;Σ     (empty)
                ⇒       f64  ⊣  (empty)
              (Lit) §5.8
                e       0.0
                Γ;Σ     (empty)
                ⇒       f64  ⊣  (empty)

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.
`#n` after an aggregate is its identity, minted when it was built; a `†`
cell is a retired binding or an identity's reserved slot (`introVal`).

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i32  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    1
         store   (empty)
           →     (empty)
         result  value 1
  [3]   d3        (Dbg) §5.8, the observable output of §6.12
         expr    @dbg(1)
         store   (empty)
           →     (empty)
         events  >> @dbg prints 1
         result  value ()
  [4]   d5            literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [5]   d5            literal §6.3 (3.12:9 rounds the decimal)
         expr    0.0
         store   (empty)
           →     (empty)
         result  value 0.0
  [6]   d4          (D-Float-Arith) §6.4
         expr    (0.0 / 0.0)
         store   (empty)
           →     (empty)
         result  value NaN
  [7]   d3        (D-Float-To-Int-Trap) §6.4
         expr    @float_to_int((0.0 / 0.0))
         store   (empty)
           →     (empty)
         result  PANIC (overflow) — a defined trap, §6.12
  [8]   d2      (D-Seq) §6.7
         expr    { @dbg(1); @float_to_int((0.0 / 0.0)) }
         store   (empty)
           →     (empty)
         result  PANIC (overflow) — a defined trap, §6.12
  [9]   d0  (Panic-Lift) §6.2 — the callee trapped, so no frame is popped: §6.12 abandons the configuration and `run-all-scope-drops` never runs
         expr    f0()
         store   (empty)
           →     (empty)
         result  PANIC (overflow) — a defined trap, §6.12

────────────────────────────────────────────────────────────────────────────────
Outcome: PANIC (overflow) — a defined trap, §6.12
         trace up to the trap: @dbg prints 1

