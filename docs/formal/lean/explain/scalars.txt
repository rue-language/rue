════════════════════════════════════════════════════════════════════════════════
 scalars
════════════════════════════════════════════════════════════════════════════════
Well-typed scalar flow: a binding used twice by copy.

Rules exercised: (Use-Copy) §5.1 · (Let) §5.3

── The program ─────────────────────────────────────────────────────────────────

    {
        let v0: i64 = (2 + 3);
        (v0 + v0)
    }

── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: ACCEPTED by the checker (§5).
         type i64, outgoing Γ;Σ (empty)

         `check_sound` ties this acceptance to a real derivation, so the §7
         safety theorems apply to this program.

── The derivation (§5) ─────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order.

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: i64 = (2 + 3); (v0 + v0) }
    Γ;Σ     (empty)
    ⇒       i64  ⊣  (empty)
      (Arith) §5.8
        e       (2 + 3)
        Γ;Σ     (empty)
        ⇒       i64  ⊣  (empty)
          (Lit) §5.8
            e       2
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
          (Lit) §5.8
            e       3
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Arith) §5.8
        e       (v0 + v0)
        Γ;Σ     [v0: i64 = Owned]
        ⇒       i64  ⊣  [v0: i64 = Owned]
          (Use-Copy) §5.1
            e       v0
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]
          (Use-Copy) §5.1
            e       v0
            Γ;Σ     [v0: i64 = Owned]
            ⇒       i64  ⊣  [v0: i64 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth and `>>` marks a drop event.

  [1]   d2      literal §6.3
         expr    2
         store   (empty)
           →     (empty)
         result  value 2
  [2]   d2      literal §6.3
         expr    3
         store   (empty)
           →     (empty)
         result  value 3
  [3]   d1    (D-Arith) §6.4
         expr    (2 + 3)
         store   (empty)
           →     (empty)
         result  value 5
  [4]   d1    (D-Let) §6.7 (mint the binding)
         expr    let v0 = 5 at ℓ0
         store   (empty)
           →     [ℓ0 = 5]
         result  value 5
  [5]   d2      (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = 5]
           →     [ℓ0 = 5]
         result  value 5
  [6]   d2      (D-Use-Copy) §6.3
         expr    v0
         store   [ℓ0 = 5]
           →     [ℓ0 = 5]
         result  value 5
  [7]   d1    (D-Arith) §6.4
         expr    (v0 + v0)
         store   [ℓ0 = 5]
           →     [ℓ0 = 5]
         result  value 10
  [8]   d0  (D-EndScope) §6.7 (retire the binding)
         expr    endscope([ℓ0])
         store   [ℓ0 = 5]
           →     [ℓ0 = † (retired)]
         result  value 10

────────────────────────────────────────────────────────────────────────────────
Outcome: ok — value 10
         drop trace: (no drops)

