════════════════════════════════════════════════════════════════════════════════
 linear_half_consumed
════════════════════════════════════════════════════════════════════════════════
A linear value consumed in one arm of an if only: the §5.5 join rejects it;
dynamically it leaks on the other path.

Rules exercised: (If) §5.5 join · 3.8:50

── The program ─────────────────────────────────────────────────────────────────

@copy struct S0 { x0: i64 }
struct S1 { x0: i64 }
drop fn S1(self) { @dbg(self.x0); }
linear struct S2 { x0: i64 }
linear struct S3 { x0: i64 }
drop fn S3(self) { @dbg(self.x0); }
struct S4 { x0: i64, x1: S3 }
struct S5 { x0: i64, x1: S1 }
drop fn S5(self) { @dbg(self.x0); }
@copy struct S6 { x0: i64, x1: i64 }
struct S7 { x0: S1, x1: S1 }
struct S8 { x0: S1, x1: i64 }
struct S9 { x0: S7, x1: i64 }
struct S10 { x0: S3, x1: S1 }
fn f0() -> i64 {
    {
        let v0: S2 = S2 { x0: 9 };
        {
            let t2d: i64 = if false {
                {
                    @drop(v0);
                    0
                }
            } else {
                0
            };
            0
        }
    }
}



── What the checker says (§5) ──────────────────────────────────────────────────

Verdict: REJECTED by the checker (§5).

The premise that fails, in fn f0() -> i64, at (If) §5.5 join:

    on  if false { { @drop(v0); 0 } } else { 0 }

    the two arms disagree on a linear-carrying entry — v0: S2 is MovedOut in
    the then-arm and Owned in the else-arm, so a linear value is consumed on
    only some paths ((If) premise `Σ' = join(Σ1, Σ2)`, §5.5; 3.8:50; the
    compiler reports E0443)

── The derivations (§5) ────────────────────────────────────────────────────────

Each node is one rule; `e` is the expression it concludes about, `Γ;Σ` the
fused context flowing into it, and `⇒ T ⊣ Σ'` its conclusion. A rule's
premises are the nodes indented under it, in premise order. One tree per
function body, checked from (Fn) §5.8's entry context.

  fn f0() -> i64

  (Let) §5.3 + the §5.6 scope-exit leak check
    e       { let v0: S2 = S2 { x0: 9 }; { if false { { @drop(v0); 0 } } else { 0 }; 0 } }
    Γ;Σ     (empty)
    ✗       this premise fails:
            a premise's own derivation failed — the reason is the rejected
            premise nested under this rule
      (Struct-Intro) §5.8
        e       S2 { x0: 9 }
        Γ;Σ     (empty)
        ⇒       S2  ⊣  (empty)
          (Lit) §5.8
            e       9
            Γ;Σ     (empty)
            ⇒       i64  ⊣  (empty)
      (Seq) §5.3, 3.8:64
        e       { if false { { @drop(v0); 0 } } else { 0 }; 0 }
        Γ;Σ     [v0: S2 = Owned]
        ✗       this premise fails:
                a premise's own derivation failed — the reason is the rejected
                premise nested under this rule
          (If) §5.5 join
            e       if false { { @drop(v0); 0 } } else { 0 }
            Γ;Σ     [v0: S2 = Owned]
            ✗       this premise fails:
                    the two arms disagree on a linear-carrying entry — v0: S2 is
                    MovedOut in the then-arm and Owned in the else-arm, so a linear
                    value is consumed on only some paths ((If) premise `Σ' = join(Σ1,
                    Σ2)`, §5.5; 3.8:50; the compiler reports E0443)
              (Lit) §5.8
                e       false
                Γ;Σ     [v0: S2 = Owned]
                ⇒       bool  ⊣  [v0: S2 = Owned]
              (Seq) §5.3, 3.8:64
                e       { @drop(v0); 0 }
                Γ;Σ     [v0: S2 = Owned]
                ⇒       i64  ⊣  [v0: S2 = MovedOut]
                  (@Drop) §5.3
                    e       @drop(v0)
                    Γ;Σ     [v0: S2 = Owned]
                    ⇒       ()  ⊣  [v0: S2 = MovedOut]
                  (Lit) §5.8
                    e       0
                    Γ;Σ     [v0: S2 = MovedOut]
                    ⇒       i64  ⊣  [v0: S2 = MovedOut]
              (Lit) §5.8
                e       0
                Γ;Σ     [v0: S2 = Owned]
                ⇒       i64  ⊣  [v0: S2 = Owned]

── The run (§6) ────────────────────────────────────────────────────────────────

One row per evaluated node, in execution order: a node's premises run before
the node itself, so the table reads top to bottom as the machine ran. `d<n>`
is the nesting depth — a callee's rows are deeper than its call's — and `>>`
marks a drop event. The run enters at f0(), as `Dynamics.run` does.

  [1]   d1    (D-Call) §6.9 (push the frame)
         expr    fn f0() -> i64  with []
         store   (empty)
           →     (empty)
         result  value ()
  [2]   d4          literal §6.3
         expr    9
         store   (empty)
           →     (empty)
         result  value 9
  [3]   d3        (D-Struct) §6.5 (mint #0)
         expr    S2 { x0: 9 }
         store   (empty)
           →     [ℓ0 = †]
         result  value S2 { 9 }#0
  [4]   d3        (D-Let) §6.7 (mint the binding)
         expr    let v0 = S2 { 9 }#0 at ℓ1
         store   [ℓ0 = †]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value S2 { 9 }#0
  [5]   d5            literal §6.3
         expr    false
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value false
  [6]   d5            literal §6.3
         expr    0
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value 0
  [7]   d4          (D-If-F) §6.6
         expr    if false { { @drop(v0); 0 } } else { 0 }
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value 0
  [8]   d4          literal §6.3
         expr    0
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value 0
  [9]   d3        (D-Seq) §6.7
         expr    { if false { { @drop(v0); 0 } } else { 0 }; 0 }
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  value 0
  [10]  d2      (D-EndScope) §6.7 (retire the binding)
         expr    { let v0: S2 = S2 { x0: 9 }; { if false { { @drop(v0); 0 } } else { 0 }; 0 } }
         store   [ℓ0 = †, ℓ1 = S2 { 9 }#0]
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)
  [11]  d0  (D-Return-Value) §6.9 (pop the frame)
         expr    f0()
         store   (empty)
           →     [ℓ0 = †, ℓ1 = S2 { 9 }#0]
         result  REFUSED (linearLeak)
                 a scope exit or a frame teardown reached a live linear value: a
                 linear obligation was never discharged (endscope §6.7 /
                 `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
                 “consumed exactly once”)

────────────────────────────────────────────────────────────────────────────────
Outcome: REFUSED (linearLeak)
         a scope exit or a frame teardown reached a live linear value: a
         linear obligation was never discharged (endscope §6.7 /
         `run-all-scope-drops` §6.9, the §5.6 leak check executed; 3.8:32; §7
         “consumed exactly once”)

