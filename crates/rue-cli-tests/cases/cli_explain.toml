[section]
id = "cli.explain"
name = "Compiler-owned error explanations"
description = """
RUE-437: `rue explain E####` is a non-compiling CLI command backed by the
canonical rue-error metadata identity. The deliberately invalid fixture proves
the successful command returns before source loading or compilation.
"""

[[case]]
name = "covered_code_is_explained_without_compiling"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0201"]
compile_only = true
compile_stdout_contains = [
  "E0201: Undefined variable",
  "Likely cause:",
  "Undefined local:",
  "Define the name before use:",
  "docs/spec/src/10-modules/03-visibility.md (spec rule 10.3:8)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "lexer_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0011"]
compile_only = true
compile_stdout_contains = [
  "E0011: Malformed float literal",
  "Leading dot diagnosed by the lexer:",
  "Trailing dot diagnosed by the parser:",
  "docs/spec/src/02-lexical-structure/_index.md (spec rule 2.0:2)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "lexer_budget_summary_is_explained_without_compiling"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0010"]
compile_only = true
compile_stdout_contains = [
  "E0010: Lexer diagnostics omitted",
  "after retaining its first 100 lexical diagnostics",
  "More than 100 lexical errors:",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "parser_unexpected_eof_uses_the_active_unexpected_token_code"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0100"]
compile_only = true
compile_stdout_contains = [
  "E0100: Unexpected token",
  "End-of-file is treated as a token",
  "Unexpected end of file:",
  "docs/spec/src/06-items/01-functions.md (spec rule 6.1:2)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "struct_field_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0400"]
compile_only = true
compile_stdout_contains = [
  "E0400: Missing fields",
  "Omitted struct field:",
  "Initialize every field:",
  "docs/spec/src/03-types/06-struct-types.md (spec rule 3.6:5)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "parser_budget_summary_is_explained_without_compiling"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0103"]
compile_only = true
compile_stdout_contains = [
  "E0103: Parser diagnostics omitted",
  "first 100 diagnostics selected by the parser's diagnostic policy",
  "More than 100 parser errors:",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "linear_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0406"]
compile_only = true
compile_stdout_contains = [
  "E0406: Linear value not consumed",
  "Unconsumed linear value:",
  "Pass the value to a consumer:",
  "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:32)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "method_call_form_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0414"]
compile_only = true
compile_stdout_contains = [
  "E0414: Method called as assoc fn",
  "Method called through its type:",
  "Call the method on a receiver:",
  "docs/spec/src/06-items/04-impl-blocks.md (spec rule 6.4:23)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "enum_pattern_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0421"]
compile_only = true
compile_stdout_contains = [
  "E0421: Unknown enum type",
  "Unknown enum type in a pattern:",
  "Use the resolved enum type:",
  "docs/spec/src/06-items/03-enums.md (spec rule 6.3:4)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "borrow_exclusivity_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0430"]
compile_only = true
compile_stdout_contains = [
  "E0430: Borrow inout conflict",
  "Borrow and mutate fields of one root:",
  "Share the same root read-only:",
  "docs/spec/src/06-items/01-functions.md (spec rule 6.1:30)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "path_sensitive_linear_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0443"]
compile_only = true
compile_stdout_contains = [
  "E0443: Linear value not consumed on all paths",
  "Consume a linear value in only one branch:",
  "Consume the linear value in every branch:",
  "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:50)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "destructor_field_move_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0456"]
compile_only = true
compile_stdout_contains = [
  "E0456: Move field out of destructor type",
  "Move a field away from a destructor-bearing value:",
  "Read Copy data without moving the field:",
  "docs/spec/src/03-types/09-destructors.md (spec rule 3.9:34)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "copy_destructor_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0457"]
compile_only = true
compile_stdout_contains = [
  "E0457: Copy struct with destructor",
  "Give a Copy struct a destructor:",
  "Keep the destructor on a move-only struct:",
  "docs/spec/src/03-types/09-destructors.md (spec rule 3.9:31)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "private_type_constructor_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0460"]
compile_only = true
compile_stdout_contains = [
  "E0460: Private unqualified access",
  "Apply a private type constructor across directories:",
  "// --- sub/lib.rue",
  "Export the type constructor:",
  "docs/spec/src/10-modules/04-module-bindings.md (spec rule 10.4:16)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "const_initializer_cycle_code_uses_the_same_compiler_owned_query"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0461"]
compile_only = true
compile_stdout_contains = [
  "E0461: Const initializer cycle",
  "Create a constant-initializer cycle:",
  "Give the dependency chain a base value:",
  "docs/spec/src/06-items/05-constants.md (spec rule 6.5:8)",
]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "reserved_destructor_band_gap_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0458"]
compile_fail = true
error_contains = ["unknown or retired error code E0458"]

# This bounded tranche crosses ownership, arrays, parser resources, recursive
# layout, patterns, and raw pointers. Every command uses invalid source to pin
# `explain` as a deterministic, non-compiling metadata path.
[[case]]
name = "declared_linear_destructure_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0474"]
compile_only = true
compile_stdout_contains = ["E0474: Linear field dropped by destructure", "Destructure a declared-linear value with linear residue:", "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:60)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "const_annotation_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0475"]
compile_only = true
compile_stdout_contains = ["E0475: Const missing type annotation", "Omit a value constant's type:", "docs/spec/src/06-items/05-constants.md (spec rule 6.5:4)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "linear_discard_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0478"]
compile_only = true
compile_stdout_contains = ["E0478: Linear value discarded", "Discard a linear value with a wildcard binding:", "docs/spec/src/05-statements/01-let-statements.md (spec rule 5.1:16)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "partially_moved_array_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0480"]
compile_only = true
compile_stdout_contains = ["E0480: Assign to partially moved array", "when the element type carries a linear value, it is legal only after every element has first been consumed", "Write the element that was moved out:", "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:77)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "array_length_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0481"]
compile_only = true
compile_stdout_contains = ["E0481: Invalid array length", "Use a negative array length:", "docs/spec/src/03-types/05-array-types.md (spec rule 3.5:2)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "nesting_limit_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0482"]
compile_only = true
compile_stdout_contains = ["E0482: Nesting limit exceeded", "fixed syntactic nesting limit of 256 levels", "docs/spec/src/appendices/C-implementation-limits.md (spec rule C.6:3)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "recursive_type_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0483"]
compile_only = true
compile_stdout_contains = ["E0483: Recursive type infinite size", "Break recursion with pointer indirection:", "docs/spec/src/03-types/_index.md (spec rule 3.0:5)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "duplicate_pattern_binding_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0484"]
compile_only = true
compile_stdout_contains = ["E0484: Duplicate pattern binding", "Bind both payloads distinctly:", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:30)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "raw_place_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0485"]
compile_only = true
compile_stdout_contains = ["E0485: Raw requires place", "Take the address of a local place:", "docs/spec/src/09-unchecked-code/02-intrinsics.md (spec rule 9.2:6a)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "reserved_ownership_band_gaps_are_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0476"]
compile_fail = true
error_contains = ["unknown or retired error code E0476"]

[[case]]
name = "second_reserved_ownership_band_gap_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0477"]
compile_fail = true
error_contains = ["unknown or retired error code E0477"]

[[case]]
name = "third_reserved_ownership_band_gap_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0479"]
compile_fail = true
error_contains = ["unknown or retired error code E0479"]

# This bounded tranche covers linear payloads, second-class slices, field
# pointers, parameters, fixed/growable strings, linear overwrite, and the
# owning-container element gate. Invalid source pins `explain` as a metadata
# path; each assertion also names the corrected compiler-validated control.
[[case]]
name = "linear_payload_discard_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0486"]
compile_only = true
compile_stdout_contains = ["E0486: Linear payload discarded", "Discard a linear match payload:", "Bind and consume the linear payload:", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:30)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "slice_return_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0487"]
compile_only = true
compile_stdout_contains = ["E0487: Slice return not allowed", "Return a second-class slice:", "Use the slice within the call:", "docs/spec/src/07-arrays/02-slices.md (spec rule 7.2:4)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "slice_aggregate_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0488"]
compile_only = true
compile_stdout_contains = ["E0488: Slice in aggregate field", "Store a slice in a struct:", "Store an owning fixed array:", "docs/spec/src/07-arrays/02-slices.md (spec rule 7.2:5)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "slice_escape_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0489"]
compile_only = true
compile_stdout_contains = ["E0489: Slice escapes scope", "Bind a slice to a local:", "Read a slice parameter directly:", "docs/spec/src/07-arrays/02-slices.md (spec rule 7.2:6)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "field_pointer_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0490"]
compile_only = true
compile_stdout_contains = ["E0490: Field ptr requires field", "Request a field pointer for a whole value:", "Request a pointer to a named field:", "docs/spec/src/09-unchecked-code/02-intrinsics.md (spec rule 9.2:17)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "duplicate_parameter_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0491"]
compile_only = true
compile_stdout_contains = ["E0491: Duplicate parameter", "Repeat a function parameter name:", "Give parameters distinct names:", "docs/spec/src/06-items/01-functions.md (spec rule 6.1:34)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "fixed_string_capacity_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0492"]
compile_only = true
compile_stdout_contains = ["E0492: Str fixed capacity exceeded", "Exceed a fixed string capacity:", "Choose sufficient fixed capacity:", "docs/spec/src/03-types/07-string-type.md (spec rule 3.7:51)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "linear_overwrite_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0493"]
compile_only = true
compile_stdout_contains = ["E0493: Linear value overwritten", "Overwrite a live linear local:", "Reinitialize after consuming the old value:", "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:77)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "linear_inout_overwrite_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0494"]
compile_only = true
compile_stdout_contains = ["E0494: Linear value overwritten through inout", "Overwrite a linear inout parameter:", "Mutate a non-linear field through inout:", "docs/spec/src/03-types/08-move-semantics.md (spec rule 3.8:77)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "buffer_first_class_string_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0495"]
compile_only = true
compile_stdout_contains = ["E0495: Buffer not first class str", "Pass a buffer as first-class str:", "Borrow the buffer for string access:", "docs/spec/src/03-types/07-string-type.md (spec rule 3.7:59)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "inout_string_provenance_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0496"]
compile_only = true
compile_stdout_contains = ["E0496: Inout str requires local buffer", "Request an exclusive view of static str:", "Use a local buffer for an exclusive view:", "docs/spec/src/03-types/07-string-type.md (spec rule 3.7:60)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "string_view_escape_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0497"]
compile_only = true
compile_stdout_contains = ["E0497: Str view not first class", "Return a borrowed string view:", "Read the string view within its call:", "docs/spec/src/03-types/07-string-type.md (spec rule 3.7:59)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "retired_container_destructor_code_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0498"]
compile_fail = true
error_contains = ["unknown or retired error code E0498"]

[[case]]
name = "linear_container_element_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0499"]
compile_only = true
compile_stdout_contains = ["E0499: Container element is linear", "Instantiate a droppable-gated container with a linear element:", "Use a non-linear container element:", "docs/spec/src/04-expressions/13-intrinsics.md (spec rule 4.13:5a)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

# The active control-flow tranche covers loop targets and the exact trusted
# Option/Result rules for `?`. Invalid source pins every `explain` invocation
# as a non-compiling metadata path.
[[case]]
name = "break_outside_loop_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0500"]
compile_only = true
compile_stdout_contains = ["E0500: Break outside loop", "Break from a function body:", "Break from an enclosing loop:", "docs/spec/src/04-expressions/08-loop-expressions.md (spec rule 4.8:9)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "continue_outside_loop_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0501"]
compile_only = true
compile_stdout_contains = ["E0501: Continue outside loop", "Continue from a function body:", "Continue an enclosing loop:", "docs/spec/src/04-expressions/08-loop-expressions.md (spec rule 4.8:9)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "break_with_value_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0502"]
compile_only = true
compile_stdout_contains = ["E0502: Break with value", "Break with a value operand:", "Store the value before breaking:", "docs/spec/src/04-expressions/08-loop-expressions.md (spec rule 4.8:22)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "question_outside_option_function_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0503"]
compile_only = true
compile_stdout_contains = ["E0503: Question outside option fn", "Propagate Option from a plain-value function:", "Return trusted Option before propagating:", "docs/spec/src/04-expressions/15-try-expressions.md (spec rule 4.15:4)", "docs/spec/src/04-expressions/15-try-expressions.md (spec rule 4.15:7)"]
compile_stdout_not_contains = ["Compiled", "error conversion"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "question_on_non_option_or_result_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0504"]
compile_only = true
compile_stdout_contains = ["E0504: Question on non option", "neither an exact specialization", "Apply try to a plain integer:", "Apply try to a trusted Option:", "docs/spec/src/04-expressions/15-try-expressions.md (spec rule 4.15:3)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "question_outside_result_function_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0505"]
compile_only = true
compile_stdout_contains = ["E0505: Question outside result fn", "Propagate Result from a plain-value function:", "Return trusted Result before propagating:", "docs/spec/src/04-expressions/15-try-expressions.md (spec rule 4.15:4)"]
compile_stdout_not_contains = ["Compiled", "error conversion"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "question_result_error_type_mismatch_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0506"]
compile_only = true
compile_stdout_contains = ["E0506: Question err type mismatch", "does not perform error conversion", "Propagate a different Result error type:", "Use the same Result error type:", "docs/spec/src/04-expressions/15-try-expressions.md (spec rule 4.15:4)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

# The active match band documents exhaustiveness, the zero-arm exception, and
# the scrutinee types accepted by semantic analysis. Invalid source pins each
# invocation as a non-compiling metadata path.
[[case]]
name = "non_exhaustive_match_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0600"]
compile_only = true
compile_stdout_contains = ["E0600: Non exhaustive match", "Omit one boolean case:", "Cover both boolean cases:", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:9)", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:10)", "docs/spec/src/06-items/03-enums.md (spec rule 6.3:21)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "empty_match_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0601"]
compile_only = true
compile_stdout_contains = ["E0601: Empty match", "Leave an integer match empty:", "Add an exhaustive wildcard arm:", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:26)", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:27)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "invalid_match_type_code_is_explained"
files = [{ path = "main.rue", source = "fn # this source must never be parsed\n" }]
args = ["explain", "E0602"]
compile_only = true
compile_stdout_contains = ["E0602: Invalid match type", "Match a string value:", "Match a supported integer value:", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:2)", "docs/spec/src/04-expressions/07-match-expressions.md (spec rule 4.7:13)"]
compile_stdout_not_contains = ["Compiled"]
compile_stderr_not_contains = ["main.rue", "error[E"]

[[case]]
name = "malformed_code_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E201"]
compile_fail = true
error_contains = ["must have the form E followed by exactly four decimal digits"]
compile_stdout_not_contains = ["E0201:"]

[[case]]
name = "unknown_code_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E9999"]
compile_fail = true
error_contains = ["unknown or retired error code E9999"]

[[case]]
name = "retired_code_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0005"]
compile_fail = true
error_contains = ["unknown or retired error code E0005"]

[[case]]
name = "known_but_uncovered_code_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0250"]
compile_fail = true
error_contains = ["no detailed explanation is available yet for E0250"]

[[case]]
name = "extra_argument_is_rejected"
files = [{ path = "main.rue", source = "fn main() {}\n" }]
args = ["explain", "E0201", "main.rue"]
compile_fail = true
error_contains = ["requires exactly one error code", "Usage: rue explain <E####>"]
