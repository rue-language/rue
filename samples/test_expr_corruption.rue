// Test if expression corrupts variables
fn main() {
    let a: i64 = 42;
    let b: i64 = 17;
    
    println_i64(b);  // Should print 17
    
    let c = a + b;   // This expression might corrupt b
    
    println_i64(b);  // Should still print 17
}