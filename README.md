# Pull-request assets

Screenshots referenced from pull-request descriptions, kept off `trunk` on
purpose: they document a review, not the product, and the repository has no
other committed images outside brand assets.

This branch is an orphan. It shares no history with `trunk` and is never
merged. Directories are named for the issue whose pull request uses them, and
can be deleted once that pull request is closed.
